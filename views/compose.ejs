<%- include('./partials/header') %>

<link href="https://cdn.quilljs.com/1.3.7/quill.snow.css" rel="stylesheet">
<style>
.ql-snow .ql-picker.ql-size .ql-picker-label::before,
.ql-snow .ql-picker.ql-size .ql-picker-item::before { content: attr(data-value) !important; }
.ql-size-10px { font-size: 10px; } .ql-size-12px { font-size: 12px; }
.ql-size-14px { font-size: 14px; } .ql-size-16px { font-size: 16px; }
.ql-size-18px { font-size: 18px; } .ql-size-20px { font-size: 20px; }
.ql-size-24px { font-size: 24px; } .ql-size-28px { font-size: 28px; }
.ql-size-32px { font-size: 32px; }
.ql-editor img { max-width: 100%; height: auto; border-radius: 4px; }

.jenis-tabs { display:flex; gap:0; border:1px solid var(--gray-200); border-radius:8px; overflow:hidden; margin-top:2px; }
.jenis-tab {
  flex:1; padding:9px 0; font-size:13px; font-weight:500; text-align:center; cursor:pointer;
  background:var(--gray-50); color:var(--gray-500); border:none; transition:all .15s;
  display:flex; align-items:center; justify-content:center; gap:6px;
}
.jenis-tab.active { background:#071840; color:#fff; }
.jenis-tab:first-child { border-right:1px solid var(--gray-200); }

.ext-recipient-input { display:flex; gap:8px; margin-bottom:8px; }
.ext-recipient-input input { flex:1; }
.ext-list { display:flex; flex-direction:column; gap:6px; }
.ext-item {
  display:flex; align-items:center; gap:8px;
  padding:7px 12px; border-radius:7px;
  background:var(--gray-50); border:1px solid var(--gray-200); font-size:13px;
}
.ext-item-name { font-weight:600; color:var(--gray-800); }
.ext-item-email { color:var(--gray-500); }
.ext-item-del { margin-left:auto; background:none; border:none; cursor:pointer; color:var(--gray-400); font-size:16px; line-height:1; }
.ext-item-del:hover { color:var(--red-500); }

.ql-container { font-family:'Times New Roman',Times,serif; font-size:14px; border:1px solid var(--gray-200) !important; border-top:0 !important; border-radius:0 0 var(--radius-md) var(--radius-md) !important; }
.ql-toolbar { border:1px solid var(--gray-200) !important; border-radius:var(--radius-md) var(--radius-md) 0 0 !important; background:var(--gray-50); flex-wrap:wrap; }
.ql-editor { min-height:220px; line-height:1.8; color:var(--gray-800); padding:16px; }
.ql-editor.ql-blank::before { color:var(--gray-400); font-style:normal; font-size:13.5px; }
.ql-toolbar .ql-formats { margin-right:8px; margin-bottom:4px; }
.ql-snow .ql-picker { color:var(--gray-600); }
.ql-snow .ql-picker.ql-size { width:80px; }

/* Tipe surat section badges */
.sec-header {
  display:flex; align-items:center; gap:8px; margin:10px 0 2px;
  padding:0 0 8px; background:none; border:none;
  border-bottom:1px solid var(--gray-200); border-radius:0;
  font-size:13.5px; font-weight:700; color:var(--gray-800);
}
.sec-badge { display:none; }

/* Dynamic row table */
.dyn-table { width:100%; border-collapse:collapse; margin-top:8px; }
.dyn-table th { font-size:11px; font-weight:700; text-transform:uppercase; letter-spacing:.5px; color:var(--gray-500); padding:6px 8px; border-bottom:2px solid var(--gray-200); text-align:left; }
.dyn-table td { padding:5px 4px; vertical-align:middle; }
.dyn-table td input, .dyn-table td select { font-size:12.5px; padding:5px 8px; }
.dyn-row-del { background:none; border:none; cursor:pointer; color:var(--gray-400); font-size:16px; padding:2px 6px; }
.dyn-row-del:hover { color:var(--red-500); }
.btn-add-row { margin-top:6px; font-size:12px; padding:5px 12px; background:var(--gray-50); border:1px solid var(--gray-200); border-radius:6px; cursor:pointer; color:var(--gray-600); }
.btn-add-row:hover { background:var(--gray-100); }

/* Lebih lega & tidak berdesakan */
.compose-form { gap:20px; padding:24px; }
#sec-memo, #sec-naskah { display:flex; flex-direction:column; gap:16px; }
#sec-memo .form-label, #sec-naskah .form-label { margin-bottom:2px; }

/* Seksi lipat (collapsible) — ringan, tidak ramai */
details.cgroup { border:1px solid var(--gray-150); border-radius:8px; overflow:hidden; }
details.cgroup > summary { list-style:none; cursor:pointer; padding:11px 14px; background:#fff; font-size:13px; font-weight:500; color:var(--gray-600); display:flex; align-items:center; gap:8px; }
details.cgroup > summary:hover { background:var(--gray-50); }
details.cgroup[open] > summary { border-bottom:1px solid var(--gray-150); }
details.cgroup > summary::-webkit-details-marker { display:none; }
details.cgroup > summary::before { content:'▸'; color:var(--gray-400); transition:transform .15s; font-size:11px; }
details.cgroup[open] > summary::before { transform:rotate(90deg); }
details.cgroup > .cgroup-body { padding:16px; }
.cgroup-hint { font-size:11px; color:var(--gray-400); font-weight:400; }

/* Dynamic list */
.dyn-list { display:flex; flex-direction:column; gap:6px; margin-top:8px; }
.dyn-list-item { display:flex; gap:8px; align-items:center; }
.dyn-list-item input { flex:1; font-size:13px; }
.dyn-list-item .item-num { font-size:12px; font-weight:700; color:var(--gray-500); min-width:24px; text-align:right; }
</style>

<div class="page-header">
  <div class="page-title-group">
    <div class="page-breadcrumb"><a href="/inbox">Dashboard</a> / <a href="/compose">Manajemen Dokumen</a> / Buat Baru</div>
    <h1 class="page-title">Buat Dokumen Baru</h1>
    <p class="page-subtitle">Nota Dinas, Surat Dinas<% if(user && (['superadmin','admin','direktur'].includes(user.role))){ %>, MoU, MoA, PKS, SPK, SK, dan dokumen khusus lainnya<% } %></p>
  </div>
</div>

<form action="/compose" method="POST" id="composeForm" enctype="multipart/form-data">
  <input type="hidden" name="action" id="formAction" value="send">
  <input type="hidden" name="body" id="bodyHidden">
  <input type="hidden" name="jenis" id="jenisHidden" value="<%= jenisDefault || 'internal' %>">
  <input type="hidden" name="externalRecipients" id="externalRecipientsHidden" value="[]">
  <input type="hidden" name="tipeSurat" id="tipeSuratHidden" value="Surat">
  <input type="hidden" name="suratData" id="suratDataHidden" value="{}">

<div class="compose-layout">
  <div class="compose-card">
    <div class="compose-card-header">
      <span class="compose-card-title">Detail Dokumen</span>
      <div style="display:flex;align-items:center;gap:12px;">
        <label style="display:flex;align-items:center;gap:6px;font-size:12.5px;color:var(--gray-600);cursor:pointer;">
          <input type="checkbox" name="sumberTemplateCheck" id="sumberTemplateCheck" onchange="toggleSumberTemplate(this.checked)" style="width:14px;height:14px;">
          Template Eksternal
        </label>
      </div>
    </div>
    <input type="hidden" name="sumberTemplate" id="sumberTemplateHidden" value="internal">
    <div class="compose-form">

      <!-- TIPE DOKUMEN + KODE DIREKTORAT -->
      <div style="display:grid;grid-template-columns:1fr 1fr;gap:14px;">
        <div class="form-group">
          <label class="form-label">Jenis Dokumen <span class="req">*</span></label>
          <div class="select-wrap">
            <select id="tipeSuratSelect" class="form-control form-select" onchange="onTipeChange(this.value)">
              <% if (jenisDefault !== 'eksternal') { %>
              <optgroup label="— Internal —">
                <option value="Nota Dinas" selected>Nota Dinas</option>
              </optgroup>
              <% } %>
              <% if (jenisDefault === 'eksternal') { %>
              <optgroup label="— Eksternal —">
                <option value="Surat Eksternal" selected>Surat Eksternal</option>
              </optgroup>
              <% } %>
            </select>
          </div>
        </div>
        <div class="form-group">
          <label class="form-label" id="kodeDirLabel">Kode Direksi <span class="req">*</span></label>
          <div class="select-wrap">
            <select name="kodeDir" id="kodeDirSel" class="form-control form-select">
              <% const kodeDirOpts=[
                {v:'KOM',  l:'KOM — Dewan Komisaris', group:'divisi'},
                {v:'CEO',  l:'CEO — Direktur Utama', group:'direksi'},
                {v:'CTO',  l:'CTO — Direktur Teknik', group:'direksi'},
                {v:'CMO',  l:'CMO — Direktur Marketing', group:'direksi'},
                {v:'COO',  l:'COO — Direktur Operasional', group:'direksi'},
                {v:'PLAN', l:'PLAN — Business Strategy &amp; Finance', group:'divisi'},
                {v:'TECH', l:'TECH — Technical &amp; Operations', group:'divisi'},
                {v:'MP',   l:'MP — Marketing &amp; Partnership', group:'divisi'},
              ]; %>
              <% kodeDirOpts.forEach(function(o){ if(allowedKodeDir.includes(o.v)){ %>
              <option value="<%= o.v %>" data-group="<%= o.group %>" <%= (user.kodeDir===o.v||(allowedKodeDir.length===1&&allowedKodeDir[0]===o.v))?'selected':'' %>><%- o.l %></option>
              <% }}); %>
            </select>
          </div>
        </div>
      </div>

      <!-- PENGIRIM RESMI -->
      <div class="form-group">
        <label class="form-label">Pengirim Resmi <span style="font-size:11px;color:var(--gray-400);font-weight:400;">(nama yang tertera di dokumen)</span></label>
        <% if (user.role === 'direktur') { %>
        <input type="text" name="pengirimResmi" class="form-control" value="<%= user.name %>" readonly style="background:var(--gray-50);color:var(--gray-500);">
        <small class="form-hint">Sebagai direktur, surat hanya dapat dibuat atas nama diri sendiri.</small>
        <% } else if (['admin','superadmin'].includes(user.role)) { %>
        <div style="display:grid;grid-template-columns:1fr 1fr;gap:10px;">
          <input type="text" name="pengirimResmi" id="pengirimResmiInput" class="form-control" value="<%= user.name %>" placeholder="Nama pengirim resmi di surat">
          <select id="pengirimPicker" class="form-control form-select" onchange="document.getElementById('pengirimResmiInput').value=this.value;this.value=''">
            <option value="">— Pilih dari daftar karyawan —</option>
            <% groupUsersByDir(pengirimUsers, direktorats).forEach(function(g){ %>
            <optgroup label="<%= g.label %>">
              <% g.users.forEach(u => { %><option value="<%= u.name %>"><%= u.enik ? u.enik + ' — ' : '' %><%= u.name %><%= u.jabatan ? ' (' + u.jabatan + ')' : '' %></option><% }) %>
            </optgroup>
            <% }) %>
          </select>
        </div>
        <small class="form-hint">Admin dapat membuat surat atas nama direktur atau komisaris. Pembuat: <strong><%= user.name %></strong></small>
        <% } else { %>
        <div style="display:grid;grid-template-columns:1fr 1fr;gap:10px;">
          <input type="text" name="pengirimResmi" id="pengirimResmiInput" class="form-control" value="<%= user.name %>" placeholder="Nama pengirim resmi di surat">
          <% if (pengirimUsers.length > 0) { %>
          <select id="pengirimPicker" class="form-control form-select" onchange="document.getElementById('pengirimResmiInput').value=this.value;this.value=''">
            <option value="">— Pilih dari daftar karyawan —</option>
            <% groupUsersByDir(pengirimUsers, direktorats).forEach(function(g){ %>
            <optgroup label="<%= g.label %>">
              <% g.users.forEach(u => { %><option value="<%= u.name %>"><%= u.enik ? u.enik + ' — ' : '' %><%= u.name %><%= u.jabatan ? ' (' + u.jabatan + ')' : '' %></option><% }) %>
            </optgroup>
            <% }) %>
          </select>
          <% } else { %>
          <input type="text" class="form-control" disabled placeholder="Tidak ada karyawan lain di direktorat ini" style="background:var(--gray-50);color:var(--gray-400);">
          <% } %>
        </div>
        <small class="form-hint">Hanya dapat memilih pengirim dari direktorat yang sama. Pembuat: <strong><%= user.name %></strong><%= user.enik ? ' (ENIK: ' + user.enik + ')' : '' %></small>
        <% } %>
      </div>

      <!-- Info jika template eksternal -->
      <div id="eksternalTemplateInfo" style="display:none;padding:14px 16px;background:#eff6ff;border-radius:8px;border:1px solid #bfdbfe;margin-bottom:4px;">
        <div style="font-weight:600;color:#1d4ed8;font-size:13.5px;margin-bottom:4px;">Template Eksternal</div>
        <div style="font-size:13px;color:#3b82f6;">Isi perihal dan nomor surat akan di-generate otomatis. Tidak perlu mengisi form konten.</div>
      </div>

      <!-- FORM PIHAK KEDUA — hanya untuk direktur+admin & dok. khusus -->
      <% if (user && (['superadmin','admin','direktur'].includes(user.role))) { %>
      <div id="sec-pihak-kedua-form" style="display:none;background:var(--gray-50);border:1px solid var(--gray-200);border-radius:10px;padding:16px;margin-bottom:4px;">
        <div class="sec-header" style="margin-bottom:12px;">
          <span class="sec-badge">PIHAK KEDUA</span>
          Data Pihak Eksternal / Mitra
        </div>
        <div style="display:grid;grid-template-columns:1fr 1fr;gap:14px;">
          <div class="form-group">
            <label class="form-label">Pihak Pertama (Internal)</label>
            <input type="text" name="pihak1" class="form-control" value="PT Nusa Inspira Teknologi" readonly style="background:var(--gray-100);">
          </div>
          <div class="form-group">
            <label class="form-label">Pihak Kedua (Eksternal) <span class="req">*</span></label>
            <input type="text" name="pihak2" id="pihak2Input" class="form-control" placeholder="Nama mitra / vendor / instansi luar">
          </div>
          <div class="form-group">
            <label class="form-label">Nilai Kontrak (Rp)</label>
            <input type="text" name="nilaiKontrak" class="form-control" placeholder="0" oninput="formatNilai(this)">
            <small class="form-hint">Kosongkan jika tidak ada nilai</small>
          </div>
          <div class="form-group">
            <label class="form-label">Tanggal Mulai</label>
            <input type="date" name="tglMulai" class="form-control">
          </div>
          <div class="form-group">
            <label class="form-label">Tanggal Berakhir</label>
            <input type="date" name="tglBerakhir" class="form-control">
          </div>
        </div>
        <div class="form-group">
          <label class="form-label">Deskripsi / Ruang Lingkup</label>
          <textarea name="deskripsiDok" class="form-control textarea" rows="3" placeholder="Uraikan ruang lingkup, tujuan, atau klausul penting dari dokumen ini..."></textarea>
        </div>
      </div>
      <% } %>

      <div id="mainFormSections">
      <!-- SUBJEK / PERIHAL -->
      <div class="form-group">
        <label class="form-label" id="subjectLabel">Subjek / Perihal <span class="req">*</span></label>
        <input type="text" name="subject" id="subjectInput" class="form-control" placeholder="Perihal surat..." required>
      </div>

      <div style="display:grid;grid-template-columns:1fr 1fr;gap:14px;">
        <div class="form-group">
          <label class="form-label">Tag / Prioritas</label>
          <div class="select-wrap">
            <select name="tag" class="form-control form-select">
              <option value="Biasa">Biasa</option>
              <option value="Penting">Penting</option>
              <option value="Segera">Segera</option>
              <option value="Mendesak">Mendesak</option>
            </select>
          </div>
        </div>
        <div class="form-group">
          <label class="form-label">Sifat Surat</label>
          <div class="select-wrap">
            <select name="sifat" id="sifatSelect2" class="form-control form-select" onchange="updateSifat(this)">
              <% if(allowedSifat.includes('Biasa/Terbuka')){ %><option value="Biasa/Terbuka">Biasa / Terbuka</option><% } %>
              <% if(allowedSifat.includes('Segera')){ %><option value="Segera">Segera</option><% } %>
              <% if(allowedSifat.includes('Terbatas')){ %><option value="Terbatas">Terbatas</option><% } %>
              <% if(allowedSifat.includes('Rahasia')){ %><option value="Rahasia">Rahasia</option><% } %>
            </select>
          </div>
        </div>
      </div>

      <!-- ═══════════════════════════════════════════════════════ -->
      <!-- SECTION: MEMO / NOTA DINAS                             -->
      <!-- ═══════════════════════════════════════════════════════ -->
      <div id="sec-memo" style="display:none;">
        <div class="sec-header">
          <span class="sec-badge">MEMO / NOTA DINAS</span>
          Informasi Internal
        </div>
        <div style="display:grid;grid-template-columns:1fr 1fr;gap:14px;">
          <div class="form-group">
            <label class="form-label">Kepada (Jabatan / Nama)</label>
            <input type="text" id="memo_kepada" class="form-control" placeholder="cth: Kepala Bagian Keuangan">
            <select id="memo_kepada_picker" class="form-control form-select" style="margin-top:8px;" onchange="if(this.value){document.getElementById('memo_kepada').value=this.value;}this.value=''">
              <option value="">— Pilih dari daftar karyawan —</option>
              <% groupUsersByDir(pengirimUsers, direktorats).forEach(function(g){ %>
              <optgroup label="<%= g.label %>">
                <% g.users.forEach(u => { %><option value="<%= u.jabatan || u.name %>"><%= u.enik ? u.enik + ' — ' : '' %><%= u.name %><%= u.jabatan ? ' (' + u.jabatan + ')' : '' %></option><% }) %>
              </optgroup>
              <% }) %>
            </select>
          </div>
          <div class="form-group">
            <label class="form-label">Klasifikasi</label>
            <div class="select-wrap">
              <select id="memo_klasifikasi" class="form-control form-select">
                <option value="">— Pilih —</option>
                <option value="RAHASIA">RAHASIA</option>
                <option value="TERBATAS">TERBATAS</option>
                <option value="BIASA">BIASA</option>
              </select>
            </div>
          </div>
        </div>
        <!-- Nota Dinas: tabel anggaran -->
        <details id="sec-nota-budget" class="cgroup" style="display:none;">
          <summary>Rincian Anggaran / Biaya Operasional <span class="cgroup-hint">(opsional)</span></summary>
          <div class="cgroup-body">
            <table class="dyn-table" id="budgetTable">
              <thead>
                <tr>
                  <th style="width:40px;">No.</th>
                  <th>Uraian Kebutuhan</th>
                  <th style="width:160px;">Jumlah (Rp)</th>
                  <th style="width:36px;"></th>
                </tr>
              </thead>
              <tbody id="budgetBody"></tbody>
            </table>
            <button type="button" class="btn-add-row" onclick="addBudgetRow()">+ Tambah Baris</button>
          </div>
        </details>
      </div>

      <!-- ═══════════════════════════════════════════════════════ -->
      <!-- SECTION: NASKAH (Nota Dinas & Surat Eksternal)        -->
      <!-- Butir bernomor + sub-poin (a/b) + tabel acara + TTD   -->
      <!-- ═══════════════════════════════════════════════════════ -->
      <div id="sec-naskah" style="display:none;">
        <div class="sec-header">
          <span class="sec-badge">ISI NASKAH</span>
          Butir Surat & Tanda Tangan
        </div>

        <!-- Field khusus Surat Eksternal -->
        <div id="naskah-ext-fields" style="display:none;">
          <div style="display:grid;grid-template-columns:1fr 1fr;gap:14px;">
            <div class="form-group">
              <label class="form-label">Tanggal Surat</label>
              <input type="date" id="naskah_tgl_surat" class="form-control">
              <small class="form-hint">Kosongkan untuk pakai tanggal hari ini.</small>
            </div>
            <div class="form-group">
              <label class="form-label">Lampiran</label>
              <input type="text" id="naskah_lampiran" class="form-control" placeholder="cth: - / 1 berkas">
            </div>
          </div>
          <div class="form-group">
            <label class="form-label">Kepada Yth. (alamat tujuan)</label>
            <textarea id="naskah_kepada_alamat" class="form-control textarea" rows="4" placeholder="Satu baris per baris alamat, cth:&#10;Direktur Keamanan Siber dan Sandi&#10;Badan Siber dan Sandi Negara&#10;Di Tempat"></textarea>
            <small class="form-hint">Tiap baris ditampilkan sebagai baris terpisah. Kosongkan untuk pakai daftar penerima.</small>
          </div>
        </div>

        <!-- Butir bernomor -->
        <label class="form-label">Butir Surat <span style="font-size:11px;color:var(--gray-400);font-weight:400;">(bernomor 1, 2, 3 …)</span></label>
        <div id="butirList"></div>
        <button type="button" class="btn-add-row" onclick="addButir()">+ Tambah Butir</button>

        <!-- Blok tanda tangan -->
        <details class="cgroup" style="margin-top:12px;">
          <summary>Detail Tanda Tangan <span class="cgroup-hint">(Kota, Tanggal, Jabatan — opsional)</span></summary>
          <div class="cgroup-body">
            <div style="display:grid;grid-template-columns:1fr 1fr 1fr;gap:14px;">
              <div class="form-group" style="margin-bottom:0;">
                <label class="form-label">Kota TTD</label>
                <input type="text" id="naskah_ttd_kota" class="form-control" placeholder="cth: Tangerang" value="Tangerang">
              </div>
              <div class="form-group" style="margin-bottom:0;">
                <label class="form-label">Tanggal TTD</label>
                <input type="date" id="naskah_ttd_tanggal" class="form-control">
              </div>
              <div class="form-group" style="margin-bottom:0;">
                <label class="form-label">Jabatan Penandatangan</label>
                <input type="text" id="naskah_ttd_jabatan" class="form-control" placeholder="cth: EVP of Safety Operation">
              </div>
            </div>
          </div>
        </details>
        <label class="cosign-toggle" id="naskah-bsre-wrap" style="display:none;margin-top:4px;">
          <input type="checkbox" id="naskah_bsre" checked>
          Tampilkan catatan tanda tangan elektronik (BSrE) di kaki surat
        </label>
      </div>

      <!-- ═══════════════════════════════════════════════════════ -->
      <!-- SECTION: SPPD                                          -->
      <!-- ═══════════════════════════════════════════════════════ -->
      <div id="sec-sppd" style="display:none;">
        <div class="sec-header">
          <span class="sec-badge">SPPD</span>
          Data Perjalanan Dinas
        </div>
        <div style="display:grid;grid-template-columns:1fr 1fr;gap:14px;">
          <div class="form-group">
            <label class="form-label">Nama Pegawai</label>
            <input type="text" id="sppd_nama" class="form-control" placeholder="Nama lengkap pegawai">
          </div>
          <div class="form-group">
            <label class="form-label">Jabatan</label>
            <input type="text" id="sppd_jabatan" class="form-control" placeholder="Jabatan pegawai">
          </div>
          <div class="form-group">
            <label class="form-label">Tingkat / Golongan</label>
            <input type="text" id="sppd_golongan" class="form-control" placeholder="cth: Senior">
          </div>
          <div class="form-group">
            <label class="form-label">Maksud Perjalanan</label>
            <input type="text" id="sppd_maksud" class="form-control" placeholder="cth: Audit & Investigasi Internal">
          </div>
          <div class="form-group">
            <label class="form-label">Alat Angkutan</label>
            <input type="text" id="sppd_angkutan" class="form-control" placeholder="cth: Kendaraan Dinas / Umum">
          </div>
          <div class="form-group">
            <label class="form-label">Tempat Berangkat</label>
            <input type="text" id="sppd_asal" class="form-control" placeholder="cth: Jakarta">
          </div>
          <div class="form-group">
            <label class="form-label">Tempat Tujuan</label>
            <input type="text" id="sppd_tujuan" class="form-control" placeholder="cth: PT Maju Bersama, Jakarta Selatan">
          </div>
          <div class="form-group">
            <label class="form-label">Tanggal Berangkat</label>
            <input type="date" id="sppd_tgl_berangkat" class="form-control">
          </div>
          <div class="form-group">
            <label class="form-label">Tanggal Kembali</label>
            <input type="date" id="sppd_tgl_kembali" class="form-control">
          </div>
          <div class="form-group">
            <label class="form-label">Mata Anggaran</label>
            <input type="text" id="sppd_mata_anggaran" class="form-control" placeholder="cth: Biaya Operasional 2026">
          </div>
        </div>
      </div>

      <!-- ═══════════════════════════════════════════════════════ -->
      <!-- SECTION: SURAT TUGAS (PERORANGAN / TIM)               -->
      <!-- ═══════════════════════════════════════════════════════ -->
      <div id="sec-tugas" style="display:none;">
        <div class="sec-header">
          <span class="sec-badge">SURAT TUGAS</span>
          Data Pemberi & Pelaksana Tugas
        </div>
        <div style="display:grid;grid-template-columns:1fr 1fr;gap:14px;margin-bottom:14px;">
          <div class="form-group">
            <label class="form-label">Jabatan Pemberi Tugas</label>
            <div class="select-wrap">
              <select id="tugas_pemberi_preset" class="form-control form-select" onchange="onPemberiPresetChange()">
                <option value="">— Pilih jabatan —</option>
                <option value="Direktur Utama">Direktur Utama</option>
                <option value="Manajer Operasional">Manajer Operasional</option>
                <option value="HRD / Kepala SDM">HRD / Kepala SDM</option>
                <option value="lainnya">Lainnya...</option>
              </select>
            </div>
            <input type="text" id="tugas_pemberi_jabatan_custom" class="form-control" placeholder="Ketik jabatan..." style="margin-top:8px;display:none;">
          </div>
          <div class="form-group">
            <label class="form-label">Nama Pemberi Tugas</label>
            <input type="text" id="tugas_pemberi_nama" class="form-control" placeholder="Nama lengkap pemberi tugas...">
          </div>
        </div>

        <label class="form-label">Pelaksana Tugas <span style="font-size:11px;color:var(--gray-400);font-weight:400;">(bisa lebih dari satu orang)</span></label>
        <table class="dyn-table" id="tugasTable" style="margin-bottom:8px;">
          <thead>
            <tr>
              <th style="width:36px;">No.</th>
              <th>Nama Lengkap</th>
              <th style="width:200px;">Jabatan</th>
              <th style="width:36px;"></th>
            </tr>
          </thead>
          <tbody id="tugasBody"></tbody>
        </table>
        <button type="button" class="btn-add-row" onclick="addTugasRow()">+ Tambah Pelaksana</button>

        <div style="display:grid;grid-template-columns:1fr 1fr;gap:14px;margin-top:14px;">
          <div class="form-group">
            <label class="form-label">Jenis Tugas</label>
            <input type="text" id="tugas_jenis" class="form-control" placeholder="cth: Audit & Investigasi Internal">
          </div>
          <div class="form-group">
            <label class="form-label">Tujuan</label>
            <input type="text" id="tugas_tujuan" class="form-control" placeholder="cth: Investigasi Kepatuhan Operasional">
          </div>
          <div class="form-group">
            <label class="form-label">Lokasi Penugasan</label>
            <input type="text" id="tugas_lokasi" class="form-control" placeholder="cth: PT Maju Bersama, Jakarta Selatan">
          </div>
          <div class="form-group">
            <label class="form-label">Tanggal Pelaksanaan</label>
            <input type="date" id="tugas_tanggal" class="form-control">
          </div>
          <div class="form-group" style="grid-column:span 2;">
            <label class="form-label">Waktu Pelaksanaan</label>
            <input type="text" id="tugas_waktu" class="form-control" placeholder="cth: 08.00 WIB s.d. selesai" value="08.00 WIB s.d. selesai">
          </div>
        </div>
      </div>

      <!-- ═══════════════════════════════════════════════════════ -->
      <!-- SECTION: SURAT TUGAS PENDELEGASIAN                     -->
      <!-- ═══════════════════════════════════════════════════════ -->
      <div id="sec-delegasi" style="display:none;">
        <div class="sec-header">
          <span class="sec-badge">PENDELEGASIAN</span>
          Data Pelaksana Delegasi
        </div>
        <div style="display:grid;grid-template-columns:1fr 1fr;gap:14px;">
          <div class="form-group">
            <label class="form-label">Nama Pelaksana Delegasi</label>
            <input type="text" id="del_nama" class="form-control" placeholder="Nama yang menerima delegasi">
          </div>
          <div class="form-group">
            <label class="form-label">Jabatan Pelaksana</label>
            <input type="text" id="del_jabatan" class="form-control" placeholder="Jabatan pelaksana delegasi">
          </div>
        </div>
        <label class="form-label" style="margin-top:4px;">Lingkup Pendelegasian</label>
        <div class="dyn-list" id="del_lingkup_list"></div>
        <button type="button" class="btn-add-row" onclick="addDelLingkup()">+ Tambah Lingkup</button>
        <label class="form-label" style="margin-top:12px;">Ketentuan Pendelegasian <span style="font-weight:400;color:var(--gray-400);">(opsional)</span></label>
        <div class="dyn-list" id="del_ketentuan_list"></div>
        <button type="button" class="btn-add-row" onclick="addDelKetentuan()">+ Tambah Ketentuan</button>
      </div>

      <!-- ═══════════════════════════════════════════════════════ -->
      <!-- SECTION: SURAT KETERANGAN                              -->
      <!-- ═══════════════════════════════════════════════════════ -->
      <div id="sec-keterangan" style="display:none;">
        <div class="sec-header">
          <span class="sec-badge">KETERANGAN</span>
          Data Pihak Yang Diterangkan
        </div>
        <div style="display:grid;grid-template-columns:1fr 1fr;gap:14px;">
          <div class="form-group">
            <label class="form-label">Nama</label>
            <input type="text" id="ket_nama" class="form-control" placeholder="Nama yang diterangkan">
          </div>
          <div class="form-group">
            <label class="form-label">Jabatan / Posisi</label>
            <input type="text" id="ket_jabatan" class="form-control" placeholder="Jabatan dalam tim/instansi">
          </div>
          <div class="form-group">
            <label class="form-label">Tim / Kelompok <span style="font-weight:400;color:var(--gray-400);">(opsional)</span></label>
            <input type="text" id="ket_tim" class="form-control" placeholder="cth: Tim Mawar">
          </div>
          <div class="form-group">
            <label class="form-label">Keperluan / Penugasan</label>
            <input type="text" id="ket_keperluan" class="form-control" placeholder="cth: Audit Internal di PT Maju Bersama">
          </div>
          <div class="form-group">
            <label class="form-label">Lokasi</label>
            <input type="text" id="ket_lokasi" class="form-control" placeholder="cth: PT Maju Bersama, Jakarta Selatan">
          </div>
          <div class="form-group">
            <label class="form-label">Tanggal Pelaksanaan</label>
            <input type="date" id="ket_tanggal" class="form-control">
          </div>
        </div>
      </div>

      <!-- ═══════════════════════════════════════════════════════ -->
      <!-- SECTION: SURAT PERNYATAAN                              -->
      <!-- ═══════════════════════════════════════════════════════ -->
      <div id="sec-pernyataan" style="display:none;">
        <div class="sec-header">
          <span class="sec-badge">PERNYATAAN</span>
          Data Pembuat Pernyataan
        </div>
        <div style="display:grid;grid-template-columns:1fr 1fr;gap:14px;">
          <div class="form-group">
            <label class="form-label">Nama Yang Menyatakan</label>
            <input type="text" id="pry_nama" class="form-control" placeholder="Nama pembuat pernyataan">
          </div>
          <div class="form-group">
            <label class="form-label">Jabatan</label>
            <input type="text" id="pry_jabatan" class="form-control" placeholder="Jabatan">
          </div>
          <div class="form-group">
            <label class="form-label">Tim / Instansi <span style="font-weight:400;color:var(--gray-400);">(opsional)</span></label>
            <input type="text" id="pry_tim" class="form-control" placeholder="cth: Tim Mawar">
          </div>
        </div>
        <label class="form-label" style="margin-top:4px;">Poin Pernyataan</label>
        <div class="dyn-list" id="pry_poin_list"></div>
        <button type="button" class="btn-add-row" onclick="addPryPoin()">+ Tambah Poin</button>
      </div>

      <!-- ═══════════════════════════════════════════════════════ -->
      <!-- SECTION: SURAT KUASA                                   -->
      <!-- ═══════════════════════════════════════════════════════ -->
      <div id="sec-kuasa" style="display:none;">
        <div class="sec-header">
          <span class="sec-badge">SURAT KUASA</span>
          Data Penerima Kuasa
        </div>
        <div style="display:grid;grid-template-columns:1fr 1fr;gap:14px;">
          <div class="form-group">
            <label class="form-label">Nama Penerima Kuasa</label>
            <input type="text" id="kuasa_nama" class="form-control" placeholder="Nama penerima kuasa">
          </div>
          <div class="form-group">
            <label class="form-label">Jabatan</label>
            <input type="text" id="kuasa_jabatan" class="form-control" placeholder="Jabatan penerima kuasa">
          </div>
        </div>
        <label class="form-label" style="margin-top:4px;">Ruang Lingkup Kuasa</label>
        <div class="dyn-list" id="kuasa_lingkup_list"></div>
        <button type="button" class="btn-add-row" onclick="addKuasaLingkup()">+ Tambah Lingkup</button>
      </div>

      <!-- ═══════════════════════════════════════════════════════ -->
      <!-- SECTION: SURAT PENGANTAR                               -->
      <!-- ═══════════════════════════════════════════════════════ -->
      <div id="sec-pengantar" style="display:none;">
        <div class="sec-header">
          <span class="sec-badge">PENGANTAR</span>
          Daftar Dokumen / Barang yang Dikirim
        </div>
        <div class="form-group">
          <label class="form-label">Ditujukan Kepada</label>
          <input type="text" id="peng_kepada" class="form-control" placeholder="cth: Pimpinan PT Maju Bersama, Jakarta Selatan">
        </div>
        <table class="dyn-table" id="pengantarTable">
          <thead>
            <tr>
              <th style="width:36px;">No.</th>
              <th>Nama Dokumen / Barang</th>
              <th style="width:110px;">Jumlah</th>
              <th style="width:120px;">Keterangan</th>
              <th style="width:36px;"></th>
            </tr>
          </thead>
          <tbody id="pengantarBody"></tbody>
        </table>
        <button type="button" class="btn-add-row" onclick="addPengantarRow()">+ Tambah Dokumen</button>
      </div>

      <!-- ═══════════════════════════════════════════════════════ -->
      <!-- SECTION: SURAT KEPUTUSAN                               -->
      <!-- ═══════════════════════════════════════════════════════ -->
      <div id="sec-keputusan" style="display:none;">
        <div class="sec-header">
          <span class="sec-badge">SK</span>
          Konsiderans &amp; Diktum
        </div>
        <label class="form-label">Menimbang</label>
        <div class="dyn-list" id="sk_menimbang_list"></div>
        <button type="button" class="btn-add-row" onclick="addSkItem('menimbang')">+ Tambah Poin Menimbang</button>

        <label class="form-label" style="margin-top:12px;">Mengingat</label>
        <div class="dyn-list" id="sk_mengingat_list"></div>
        <button type="button" class="btn-add-row" onclick="addSkItem('mengingat')">+ Tambah Poin Mengingat</button>

        <label class="form-label" style="margin-top:12px;">Memutuskan / Menetapkan</label>
        <div class="dyn-list" id="sk_memutuskan_list"></div>
        <button type="button" class="btn-add-row" onclick="addSkItem('memutuskan')">+ Tambah Poin Keputusan</button>
      </div>

      <!-- ═══════════════════════════════════════════════════════ -->
      <!-- SECTION: SURAT UNDANGAN DINAS                          -->
      <!-- ═══════════════════════════════════════════════════════ -->
      <div id="sec-undangan" style="display:none;">
        <div class="sec-header">
          <span class="sec-badge">UNDANGAN</span>
          Detail Acara / Rapat
        </div>
        <div style="display:grid;grid-template-columns:1fr 1fr;gap:14px;">
          <div class="form-group">
            <label class="form-label">Hari / Tanggal</label>
            <input type="date" id="und_tanggal" class="form-control">
          </div>
          <div class="form-group">
            <label class="form-label">Waktu</label>
            <input type="text" id="und_waktu" class="form-control" placeholder="cth: 09.00 WIB s.d. selesai" value="09.00 WIB s.d. selesai">
          </div>
          <div class="form-group">
            <label class="form-label">Tempat</label>
            <input type="text" id="und_tempat" class="form-control" placeholder="cth: Ruang Rapat Lantai 3">
          </div>
          <div class="form-group">
            <label class="form-label">Pakaian</label>
            <input type="text" id="und_pakaian" class="form-control" placeholder="cth: Formal / Batik" value="Formal / Batik">
          </div>
          <div class="form-group" style="grid-column:span 2;">
            <label class="form-label">Agenda</label>
            <input type="text" id="und_agenda" class="form-control" placeholder="cth: Briefing & Koordinasi Tim">
          </div>
        </div>
      </div>

      <!-- ═══════════════════════════════════════════════════════ -->
      <!-- SECTION: SURAT STATUTA                                 -->
      <!-- ═══════════════════════════════════════════════════════ -->
      <div id="sec-statuta" style="display:none;">
        <div class="sec-header">
          <span class="sec-badge">STATUTA</span>
          Data Organisasi / Tim
        </div>
        <div style="display:grid;grid-template-columns:1fr 1fr;gap:14px;">
          <div class="form-group" style="grid-column:span 2;">
            <label class="form-label">Nama Organisasi / Tim</label>
            <input type="text" id="sta_nama_org" class="form-control" placeholder="cth: TIM MAWAR">
          </div>
          <div class="form-group" style="grid-column:span 2;">
            <label class="form-label">Visi <span style="font-weight:400;color:var(--gray-400);">(opsional)</span></label>
            <input type="text" id="sta_visi" class="form-control" placeholder="Pernyataan visi...">
          </div>
        </div>
        <label class="form-label" style="margin-top:4px;">Misi <span style="font-weight:400;color:var(--gray-400);">(opsional)</span></label>
        <div class="dyn-list" id="sta_misi_list"></div>
        <button type="button" class="btn-add-row" onclick="addStaMisi()">+ Tambah Misi</button>
        <label class="form-label" style="margin-top:12px;">Susunan Keanggotaan</label>
        <table class="dyn-table" id="staAnggotaTable">
          <thead>
            <tr>
              <th style="width:36px;">No.</th>
              <th>Nama</th>
              <th style="width:220px;">Jabatan dalam Tim</th>
              <th style="width:36px;"></th>
            </tr>
          </thead>
          <tbody id="staAnggotaBody"></tbody>
        </table>
        <button type="button" class="btn-add-row" onclick="addStaAnggota()">+ Tambah Anggota</button>
      </div>

      <!-- ═══════════════════════════════════════════════════════ -->
      <!-- SECTION: STANDARD LETTER RECIPIENT                     -->
      <!-- ═══════════════════════════════════════════════════════ -->
      <div id="sec-surat-ext" style="display:none;">
        <div class="sec-header">
          <span class="sec-badge">PENERIMA</span>
          Identitas Penerima Surat <span style="font-weight:400;color:var(--gray-400);font-size:12px;">(opsional, untuk surat eksternal)</span>
        </div>
        <div style="display:grid;grid-template-columns:1fr 1fr;gap:14px;">
          <div class="form-group">
            <label class="form-label">Jabatan Penerima</label>
            <input type="text" id="std_jabatan" class="form-control" placeholder="cth: Direktur Utama">
          </div>
          <div class="form-group">
            <label class="form-label">Instansi / Perusahaan</label>
            <input type="text" id="std_instansi" class="form-control" placeholder="cth: PT Maju Bersama">
          </div>
        </div>
        <div class="form-group">
          <label class="form-label">Alamat Penerima</label>
          <input type="text" id="std_alamat" class="form-control" placeholder="cth: Jl. Gatot Subroto Kav. 51, Jakarta Selatan">
        </div>
      </div>

      <!-- ═══════════════════════════════════════════════════════ -->
      <!-- SECTION: MOU / PKS / KONTRAK                           -->
      <!-- ═══════════════════════════════════════════════════════ -->
      <div id="sec-pihak-kedua" style="display:none;">
        <div class="sec-header">
          <span class="sec-badge">PIHAK KEDUA</span>
          Data Pihak Kedua
        </div>
        <div style="display:grid;grid-template-columns:1fr 1fr;gap:14px;">
          <div class="form-group">
            <label class="form-label">Nama Perwakilan</label>
            <input type="text" id="p2_nama" class="form-control" placeholder="Nama perwakilan pihak kedua">
          </div>
          <div class="form-group">
            <label class="form-label">Jabatan</label>
            <input type="text" id="p2_jabatan" class="form-control" placeholder="Jabatan pihak kedua">
          </div>
          <div class="form-group">
            <label class="form-label">Instansi / Perusahaan</label>
            <input type="text" id="p2_instansi" class="form-control" placeholder="cth: PT Maju Bersama">
          </div>
          <div class="form-group">
            <label class="form-label">Alamat</label>
            <input type="text" id="p2_alamat" class="form-control" placeholder="Alamat instansi pihak kedua">
          </div>
        </div>
        <div id="sec-nilai-kontrak" style="display:none;">
          <div class="form-group">
            <label class="form-label">Nilai Kontrak (Rp)</label>
            <input type="text" id="p2_nilai" class="form-control" placeholder="cth: 50.000.000">
          </div>
        </div>
      </div>

      <!-- ═══════════════════════════════════════════════════════ -->
      <!-- SECTION: SPK                                           -->
      <!-- ═══════════════════════════════════════════════════════ -->
      <div id="sec-spk" style="display:none;">
        <div class="sec-header">
          <span class="sec-badge">SPK</span>
          Data Pelaksana &amp; Pekerjaan
        </div>
        <div style="display:grid;grid-template-columns:1fr 1fr;gap:14px;">
          <div class="form-group">
            <label class="form-label">Nama Tim</label>
            <input type="text" id="spk_tim" class="form-control" placeholder="cth: TIM MAWAR">
          </div>
          <div class="form-group">
            <label class="form-label">Ketua Tim</label>
            <input type="text" id="spk_ketua" class="form-control" placeholder="Nama ketua tim">
          </div>
        </div>
        <div class="form-group">
          <label class="form-label">Anggota Tim</label>
          <input type="text" id="spk_anggota" class="form-control" placeholder="cth: Nama 1, Nama 2, Nama 3">
        </div>
        <label class="form-label" style="margin-top:4px;">Rincian Pekerjaan</label>
        <table class="dyn-table" id="spkTable">
          <thead>
            <tr>
              <th style="width:40px;">No.</th>
              <th>Uraian Pekerjaan</th>
              <th style="width:130px;">Keterangan</th>
              <th style="width:36px;"></th>
            </tr>
          </thead>
          <tbody id="spkBody"></tbody>
        </table>
        <button type="button" class="btn-add-row" onclick="addSpkRow()">+ Tambah Pekerjaan</button>
        <div style="display:grid;grid-template-columns:1fr 1fr 1fr;gap:14px;margin-top:12px;">
          <div class="form-group">
            <label class="form-label">Lokasi</label>
            <input type="text" id="spk_lokasi" class="form-control" placeholder="Alamat lokasi">
          </div>
          <div class="form-group">
            <label class="form-label">Tanggal</label>
            <input type="date" id="spk_tanggal" class="form-control">
          </div>
          <div class="form-group">
            <label class="form-label">Waktu</label>
            <input type="text" id="spk_waktu" class="form-control" placeholder="cth: 08.00 WIB s.d. selesai">
          </div>
        </div>
      </div>

      <!-- ═══════════════════════════════════════════════════════ -->
      <!-- SECTION: INVOICE / TAGIHAN                             -->
      <!-- ═══════════════════════════════════════════════════════ -->
      <div id="sec-invoice" style="display:none;">
        <div class="sec-header">
          <span class="sec-badge">INVOICE</span>
          Detail Tagihan
        </div>
        <div style="display:grid;grid-template-columns:1fr 1fr;gap:14px;">
          <div class="form-group">
            <label class="form-label">Tagihan Kepada (Nama Klien / Instansi)</label>
            <input type="text" id="inv_kepada" class="form-control" placeholder="cth: PT Maju Bersama">
          </div>
          <div class="form-group">
            <label class="form-label">Alamat Klien</label>
            <input type="text" id="inv_alamat" class="form-control" placeholder="cth: Jl. Sudirman No. 1, Jakarta">
          </div>
          <div class="form-group">
            <label class="form-label">Tanggal Jatuh Tempo</label>
            <input type="date" id="inv_jatuh_tempo" class="form-control">
          </div>
          <div class="form-group">
            <label class="form-label">Metode Pembayaran</label>
            <input type="text" id="inv_metode" class="form-control" placeholder="cth: Transfer Bank BCA a.n. PT NIT">
          </div>
        </div>
        <label class="form-label" style="margin-top:4px;">Rincian Item</label>
        <table class="dyn-table" id="invTable">
          <thead>
            <tr>
              <th style="width:36px;">No.</th>
              <th>Deskripsi</th>
              <th style="width:70px;">Qty</th>
              <th style="width:90px;">Satuan</th>
              <th style="width:140px;">Harga Satuan (Rp)</th>
              <th style="width:140px;">Jumlah (Rp)</th>
              <th style="width:36px;"></th>
            </tr>
          </thead>
          <tbody id="invBody"></tbody>
        </table>
        <button type="button" class="btn-add-row" onclick="addInvRow()">+ Tambah Item</button>
        <div style="display:flex;justify-content:flex-end;margin-top:14px;">
          <table style="font-size:13px;border-collapse:collapse;min-width:280px;">
            <tr>
              <td style="padding:4px 12px 4px 0;color:var(--gray-500);">Subtotal</td>
              <td style="padding:4px 0;font-weight:500;text-align:right;" id="inv_subtotal_display">Rp 0</td>
            </tr>
            <tr>
              <td style="padding:4px 12px 4px 0;color:var(--gray-500);">
                Pajak (%) <input type="number" id="inv_pajak" class="form-control" style="display:inline-block;width:60px;padding:2px 6px;font-size:12px;" placeholder="0" min="0" max="100" oninput="recalcInv()">
              </td>
              <td style="padding:4px 0;font-weight:500;text-align:right;" id="inv_pajak_display">Rp 0</td>
            </tr>
            <tr>
              <td style="padding:4px 12px 4px 0;color:var(--gray-500);">
                Diskon (Rp) <input type="text" id="inv_diskon" class="form-control" style="display:inline-block;width:100px;padding:2px 6px;font-size:12px;" placeholder="0" oninput="recalcInv()">
              </td>
              <td style="padding:4px 0;font-weight:500;text-align:right;" id="inv_diskon_display">Rp 0</td>
            </tr>
            <tr style="border-top:2px solid var(--gray-300);">
              <td style="padding:8px 12px 4px 0;font-weight:700;font-size:14px;">TOTAL</td>
              <td style="padding:8px 0 4px 0;font-weight:700;font-size:14px;text-align:right;color:var(--brand-700);" id="inv_total_display">Rp 0</td>
            </tr>
          </table>
        </div>
        <div class="form-group" style="margin-top:12px;">
          <label class="form-label">Catatan / Keterangan Pembayaran</label>
          <input type="text" id="inv_catatan" class="form-control" placeholder="cth: Mohon melakukan pembayaran sebelum tanggal jatuh tempo">
        </div>
      </div>

      <!-- ═══════════════════════════════════════════════════════ -->
      <!-- JENIS SURAT (Internal / Eksternal)                     -->
      <!-- ═══════════════════════════════════════════════════════ -->
      <% if (!hideJenisTabs) { %>
      <div class="form-group">
        <label class="form-label">Jenis Surat</label>
        <div class="jenis-tabs">
          <button type="button" class="jenis-tab active" id="tabInternal" onclick="setJenis('internal')">
            <svg width="13" height="13" viewBox="0 0 20 20" fill="none"><path d="M10 2a6 6 0 100 12A6 6 0 0010 2z" stroke="currentColor" stroke-width="1.5"/><path d="M4 18c0-3.3 2.7-6 6-6s6 2.7 6 6" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/></svg>
            Internal (Pengguna Terdaftar)
          </button>
          <button type="button" class="jenis-tab" id="tabEksternal" onclick="setJenis('eksternal')">
            <svg width="13" height="13" viewBox="0 0 20 20" fill="none"><circle cx="10" cy="10" r="8" stroke="currentColor" stroke-width="1.5"/><path d="M2 10h16M10 2c-2 2.5-3 5-3 8s1 5.5 3 8M10 2c2 2.5 3 5 3 8s-1 5.5-3 8" stroke="currentColor" stroke-width="1.5"/></svg>
            Eksternal (Email Bebas)
          </button>
        </div>
      </div>
      <% } else { %>
      <div style="padding:6px 0;font-size:12.5px;color:var(--gray-500);display:flex;align-items:center;gap:6px;">
        <% if (jenisDefault==='eksternal') { %>
        <svg width="13" height="13" viewBox="0 0 20 20" fill="none"><circle cx="10" cy="10" r="8" stroke="currentColor" stroke-width="1.5"/><path d="M2 10h16M10 2c-2 2.5-3 5-3 8s1 5.5 3 8M10 2c2 2.5 3 5 3 8s-1 5.5-3 8" stroke="currentColor" stroke-width="1.5"/></svg>
        Surat <strong>Eksternal</strong> — dikirim ke penerima di luar sistem
        <% } else { %>
        <svg width="13" height="13" viewBox="0 0 20 20" fill="none"><path d="M10 2a6 6 0 100 12A6 6 0 0010 2z" stroke="currentColor" stroke-width="1.5"/><path d="M4 18c0-3.3 2.7-6 6-6s6 2.7 6 6" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/></svg>
        Surat <strong>Internal</strong> — dikirim ke pengguna terdaftar
        <% } %>
      </div>
      <% } %>

      <!-- INTERNAL section -->
      <div id="internalSection">
        <div class="form-group">
          <label class="form-label">Tertuju (Penerima Internal)</label>
          <div class="multi-select-wrap" id="tertujuWrap">
            <div class="multi-select-input" id="tertujuInput" onclick="toggleDropdown('tertuju')">
              <span class="multi-placeholder" id="tertujuPlaceholder">Pilih penerima...</span>
              <div class="selected-tags" id="tertujuTags"></div>
              <svg width="14" height="14" viewBox="0 0 14 14" fill="none" style="flex-shrink:0;color:var(--gray-400);margin-left:auto;"><path d="M3 5l4 4 4-4" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/></svg>
            </div>
            <div class="multi-dropdown" id="tertujuDropdown">
              <div style="padding:8px;border-bottom:1px solid var(--gray-100);">
                <input type="text" class="form-control" placeholder="Cari pengguna..." style="padding:7px 10px;font-size:13px;" oninput="searchDropdown('tertuju',this.value)">
              </div>
              <% if (users.length === 0) { %>
              <div style="padding:12px 14px;color:var(--gray-400);font-size:13px;">Belum ada pengguna lain terdaftar.</div>
              <% } else { %>
              <% groupUsersByDir(users, direktorats).forEach(function(g){ %>
              <div class="dropdown-group-label"><%= g.label %></div>
              <% g.users.forEach(u => { %>
              <label class="dropdown-opt" data-search="<%= u.name.toLowerCase() %> <%= u.email.toLowerCase() %>">
                <input type="checkbox" name="to" value="<%= u._id %>" data-name="<%= u.name %>" onchange="toggleTag('tertuju',this,this.dataset.name)">
                <div>
                  <div style="font-size:13.5px;color:var(--gray-800);"><%= u.name %></div>
                  <div style="font-size:11.5px;color:var(--gray-400);"><%= u.email %> · <%= u.organization %></div>
                </div>
              </label>
              <% }) }) %>
              <% } %>
            </div>
          </div>
        </div>
        <div class="form-group">
          <label class="form-label">Tembusan / CC</label>
          <div class="multi-select-wrap" id="tembusanWrap">
            <div class="multi-select-input" id="tembusanInput" onclick="toggleDropdown('tembusan')">
              <span class="multi-placeholder" id="tembusanPlaceholder">Pilih tembusan...</span>
              <div class="selected-tags" id="tembusanTags"></div>
              <svg width="14" height="14" viewBox="0 0 14 14" fill="none" style="flex-shrink:0;color:var(--gray-400);margin-left:auto;"><path d="M3 5l4 4 4-4" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/></svg>
            </div>
            <div class="multi-dropdown" id="tembusanDropdown">
              <div style="padding:8px;border-bottom:1px solid var(--gray-100);">
                <input type="text" class="form-control" placeholder="Cari pengguna..." style="padding:7px 10px;font-size:13px;" oninput="searchDropdown('tembusan',this.value)">
              </div>
              <% if (users.length === 0) { %>
              <div style="padding:12px 14px;color:var(--gray-400);font-size:13px;">Belum ada pengguna lain.</div>
              <% } else { %>
              <% groupUsersByDir(users, direktorats).forEach(function(g){ %>
              <div class="dropdown-group-label"><%= g.label %></div>
              <% g.users.forEach(u => { %>
              <label class="dropdown-opt" data-search="<%= u.name.toLowerCase() %> <%= u.email.toLowerCase() %>">
                <input type="checkbox" name="cc" value="<%= u._id %>" data-name="<%= u.name %>" onchange="toggleTag('tembusan',this,this.dataset.name)">
                <div>
                  <div style="font-size:13.5px;color:var(--gray-800);"><%= u.name %></div>
                  <div style="font-size:11.5px;color:var(--gray-400);"><%= u.email %> · <%= u.organization %></div>
                </div>
              </label>
              <% }) }) %>
              <% } %>
            </div>
          </div>
        </div>
      </div>

      <!-- EKSTERNAL section -->
      <div id="eksternalSection" style="display:none;">
        <div class="form-group">
          <label class="form-label">Penerima (Perusahaan)</label>
          <div style="display:flex;gap:8px;margin-bottom:10px;align-items:center;">
            <select id="perusahaanPicker" class="form-control form-select" style="flex:1;">
              <option value="">— Pilih perusahaan dari daftar —</option>
            </select>
            <button type="button" class="btn btn-secondary" onclick="addExt()" style="white-space:nowrap;padding:8px 16px;">+ Tambah</button>
            <a href="/perusahaan" target="_blank" style="font-size:12px;color:var(--brand-600);white-space:nowrap;">Kelola</a>
          </div>
          <div class="ext-list" id="extList"></div>
          <div id="extEmpty" style="font-size:12.5px;color:var(--gray-400);padding:6px 0;">Belum ada penerima. Pilih perusahaan dari daftar lalu klik + Tambah.</div>
        </div>
      </div>

      <!-- ISI SURAT (Quill) -->
      <details class="cgroup" id="bodyDetails">
        <summary><span id="bodyLabel">Isi Surat / Catatan Tambahan</span> <span class="cgroup-hint">(opsional)</span></summary>
        <div class="cgroup-body">
          <div class="form-group" id="bodySection" style="margin-bottom:0;">
            <div id="quillEditor" style="min-height:160px;"></div>
          </div>
        </div>
      </details>

      <!-- LAMPIRAN -->
      <details class="cgroup">
        <summary>Lampiran <span class="cgroup-hint">(opsional — PDF, Word, Gambar, maks. 10MB)</span></summary>
        <div class="cgroup-body">
          <input type="file" name="lampiran" class="form-control" accept=".pdf,.doc,.docx,.jpg,.jpeg,.png,.gif" style="padding:6px 10px;font-size:13px;">
        </div>
      </details>
      </div><!-- /mainFormSections -->

      <div class="compose-actions">
        <button type="button" class="btn btn-ghost" onclick="submitForm('draft')">
          <svg width="14" height="14" viewBox="0 0 14 14" fill="none"><path d="M2 2h10v10H2z" stroke="currentColor" stroke-width="1.3" stroke-linejoin="round"/><path d="M4 5h6M4 7h4" stroke="currentColor" stroke-width="1.3" stroke-linecap="round"/></svg>
          Simpan Draf
        </button>
        <button type="button" class="btn btn-primary" onclick="submitForm('send')">
          <svg width="14" height="14" viewBox="0 0 14 14" fill="none"><path d="M13 1L6 8M13 1l-4 11-3-5-5-3 12-3z" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/></svg>
          Lanjut ke Preview &amp; Tanda Tangan
        </button>
      </div>
    </div>
  </div>

  <!-- Sidebar -->
  <div class="compose-sidebar">
    <div class="info-card">
      <div class="info-card-title">Info Naskah</div>
      <div class="info-item"><span class="info-key">Pengirim</span><span class="info-val"><%= user.name.split(' ')[0] %></span></div>
      <div class="info-item"><span class="info-key">Tipe</span><span class="info-val" id="infoTipe">Surat</span></div>
      <div class="info-item"><span class="info-key">Status</span><span class="info-val" style="color:var(--amber-600);">Konsep</span></div>
      <div class="info-item"><span class="info-key">Tanggal</span><span class="info-val" id="infoDate"></span></div>
      <div class="info-item"><span class="info-key">Sifat</span><span class="info-val" id="infoSifat">Biasa/Terbuka</span></div>
      <div class="info-item" style="border-bottom:none;"><span class="info-key">Penomoran</span><span class="info-val">Otomatis</span></div>
    </div>
    <% if (jenisDefault !== 'eksternal') { %>
    <div class="info-card">
      <div class="info-card-title">Format Penomoran</div>
      <div style="font-family:monospace;font-size:11px;color:var(--brand-700);background:var(--gray-50);border-radius:6px;padding:8px 10px;line-height:1.8;border:1px solid var(--gray-200);">
        <strong id="nomorPreview" style="font-size:12px;">NNN / DIV / NIT / BLN / THN</strong>
        <div style="color:var(--gray-400);font-size:10px;margin-top:2px;">Nomor urut otomatis per tahun</div>
      </div>
    </div>
    <% } %>
    <div class="info-card">
      <div class="info-card-title">Tipe Surat Tersedia</div>
      <div style="font-size:11.5px;color:var(--gray-500);line-height:1.9;">
        <div style="font-weight:600;color:var(--gray-700);margin-top:4px;">Surat Umum</div>
        <div style="padding-left:8px;">Biasa · Resmi · Formal · Edaran · Instruksi</div>
        <div style="font-weight:600;color:var(--gray-700);margin-top:4px;">Internal</div>
        <div style="padding-left:8px;">Memo · Nota Dinas · Rahasia · Konfidensial</div>
        <div style="font-weight:600;color:var(--gray-700);margin-top:4px;">Penugasan</div>
        <div style="padding-left:8px;">Surat Tugas · SPPD · Pendelegasian</div>
        <div style="font-weight:600;color:var(--gray-700);margin-top:4px;">Otoritas</div>
        <div style="padding-left:8px;">SK · Surat Kuasa · Keterangan · Pernyataan</div>
        <div style="font-weight:600;color:var(--gray-700);margin-top:4px;">Eksternal</div>
        <div style="padding-left:8px;">Pengantar · Undangan · Statuta</div>
        <div style="font-weight:600;color:var(--gray-700);margin-top:4px;">Perjanjian</div>
        <div style="padding-left:8px;">MOU · PKS · Kontrak · SPK</div>
        <div style="font-weight:600;color:var(--gray-700);margin-top:4px;">Keuangan</div>
        <div style="padding-left:8px;">Invoice / Tagihan</div>
      </div>
    </div>
    <div class="info-card">
      <div class="info-card-title">Petunjuk</div>
      <ul style="font-size:12.5px;color:var(--gray-600);line-height:1.9;padding-left:16px;">
        <li>Pilih tipe → kode &amp; berkas otomatis terisi</li>
        <li>Ubah kode jika perlu penyesuaian manual</li>
        <li>Kolom wajib bertanda <span style="color:var(--red-500);">*</span></li>
        <li>Nomor surat dibuat otomatis saat simpan</li>
      </ul>
    </div>
  </div>
</div>
</form>

<script src="https://cdn.quilljs.com/1.3.7/quill.min.js"></script>
<script>
// ── DATE ──
const today = new Date();
const mns = ['Jan','Feb','Mar','Apr','Mei','Jun','Jul','Agu','Sep','Okt','Nov','Des'];
document.getElementById('infoDate').textContent = `${today.getDate()} ${mns[today.getMonth()]} ${today.getFullYear()}`;

// ── QUILL ──
const SizeStyle = Quill.import('attributors/style/size');
SizeStyle.whitelist = ['10px','12px','14px','16px','18px','20px','24px','28px','32px'];
Quill.register(SizeStyle, true);

const quill = new Quill('#quillEditor', {
  theme: 'snow',
  placeholder: 'Tulis isi surat / instruksi tambahan di sini...',
  modules: {
    toolbar: {
      container: [
        [{ size: ['10px','12px','14px','16px','18px','20px','24px','28px','32px'] }, { header: [1,2,3,4,false] }],
        ['bold','italic','underline','strike'],
        [{ color: [] }, { background: [] }],
        ['blockquote','code-block'],
        [{ list: 'ordered' }, { list: 'bullet' }],
        [{ indent: '-1' }, { indent: '+1' }],
        [{ align: [] }],
        ['link','image','table'],
        ['clean']
      ],
      handlers: { image: imageHandler, table: tablePickerHandler }
    }
  }
});
function imageHandler() {
  const input = document.createElement('input');
  input.setAttribute('type','file'); input.setAttribute('accept','image/*'); input.click();
  input.onchange = () => {
    const file = input.files[0]; if (!file) return;
    const reader = new FileReader();
    reader.onload = e => { const r = quill.getSelection(true); quill.insertEmbed(r.index,'image',e.target.result); quill.setSelection(r.index+1); };
    reader.readAsDataURL(file);
  };
}

// ── QUILL TABLE PICKER ──
(function() {
  const style = document.createElement('style');
  style.textContent = `
  .ql-table::before { content: '⊞'; font-size: 15px; line-height: 1; }
  .tbl-picker { position:fixed; z-index:9999; background:#fff; border:1px solid var(--gray-200); border-radius:8px; padding:8px; box-shadow:0 4px 20px rgba(0,0,0,.15); }
  .tbl-picker-grid { display:grid; grid-template-columns:repeat(8,22px); gap:3px; }
  .tbl-picker-cell { width:22px; height:22px; border:1px solid var(--gray-300); border-radius:3px; cursor:pointer; background:#f9fafb; transition:background .1s,border-color .1s; }
  .tbl-picker-cell.hover { background:#dbeafe; border-color:#3b82f6; }
  .tbl-picker-label { text-align:center; font-size:11px; color:var(--gray-500); margin-top:5px; }
  `;
  document.head.appendChild(style);
})();

let _tblPicker = null;
function tablePickerHandler() {
  if (_tblPicker) { _tblPicker.remove(); _tblPicker = null; return; }
  const btn = document.querySelector('.ql-table');
  if (!btn) return;
  const rect = btn.getBoundingClientRect();
  const picker = document.createElement('div');
  picker.className = 'tbl-picker';
  picker.style.top  = (rect.bottom + 6 + window.scrollY) + 'px';
  picker.style.left = rect.left + 'px';
  const grid = document.createElement('div'); grid.className = 'tbl-picker-grid';
  const label = document.createElement('div'); label.className = 'tbl-picker-label'; label.textContent = '1 × 1';
  const ROWS = 6, COLS = 8;
  const cells = [];
  for (let r = 0; r < ROWS; r++) {
    for (let c = 0; c < COLS; c++) {
      const cell = document.createElement('div'); cell.className = 'tbl-picker-cell';
      cell.dataset.r = r; cell.dataset.c = c;
      cell.addEventListener('mouseover', () => {
        const cr = +cell.dataset.r, cc = +cell.dataset.c;
        label.textContent = `${cr+1} × ${cc+1}`;
        cells.forEach(x => {
          x.classList.toggle('hover', +x.dataset.r <= cr && +x.dataset.c <= cc);
        });
      });
      cell.addEventListener('click', () => {
        insertTable(+cell.dataset.r + 1, +cell.dataset.c + 1);
        picker.remove(); _tblPicker = null;
      });
      cells.push(cell); grid.appendChild(cell);
    }
  }
  picker.appendChild(grid); picker.appendChild(label);
  document.body.appendChild(picker); _tblPicker = picker;
  setTimeout(() => document.addEventListener('click', function _close(e) {
    if (!picker.contains(e.target) && !btn.contains(e.target)) { picker.remove(); _tblPicker = null; document.removeEventListener('click', _close); }
  }), 0);
}
function insertTable(rows, cols) {
  const cellStyle = 'border:1px solid #ccc;padding:6px 10px;min-width:60px;';
  let html = `<table style="border-collapse:collapse;width:100%;margin:8px 0;"><tbody>`;
  for (let r = 0; r < rows; r++) {
    html += '<tr>';
    for (let c = 0; c < cols; c++) {
      html += `<td style="${cellStyle}">&nbsp;</td>`;
    }
    html += '</tr>';
  }
  html += '</tbody></table><p><br></p>';
  const range = quill.getSelection(true);
  quill.clipboard.dangerouslyPasteHTML(range ? range.index : quill.getLength(), html);
}

// ── FORMAT NILAI KONTRAK ──
function formatNilai(el) {
  let v = el.value.replace(/\D/g,'');
  el.value = v ? parseInt(v).toLocaleString('id-ID') : '';
}

// ── SUMBER TEMPLATE ──
function toggleSumberTemplate(isExternal) {
  document.getElementById('sumberTemplateHidden').value = isExternal ? 'eksternal' : 'internal';
  // Sembunyikan semua section form jika eksternal (hanya perlu tipe + nomor surat)
  const formSecs = document.getElementById('mainFormSections');
  if (formSecs) formSecs.style.display = isExternal ? 'none' : '';
  const extInfo = document.getElementById('eksternalTemplateInfo');
  if (extInfo) extInfo.style.display = isExternal ? '' : 'none';
}

// ── SIFAT ──
function updateSifat(sel) {
  const v = sel.value;
  document.getElementById('infoSifat').textContent = v;
  const other = sel.id === 'sifatSelect' ? 'sifatSelect2' : 'sifatSelect';
  const otherEl = document.getElementById(other);
  if (otherEl && otherEl.value !== v) otherEl.value = v;
}

// ── TIPE SURAT ──
let currentTipe = 'Surat';
const ALL_SECS = ['sec-memo','sec-naskah','sec-sppd','sec-tugas','sec-delegasi','sec-keterangan','sec-pernyataan','sec-kuasa','sec-pengantar','sec-keputusan','sec-undangan','sec-statuta','sec-surat-ext','sec-pihak-kedua','sec-spk','sec-invoice'];
const TIPE_SECS = {
  'Surat':['sec-surat-ext'],  'Surat Resmi':['sec-surat-ext'],  'Surat Formal':['sec-surat-ext'],
  'Surat Instruksi':['sec-surat-ext'],  'Surat Edaran':[],
  'Surat Rahasia':['sec-surat-ext'], 'Surat Konfidensial':['sec-surat-ext'],
  'Surat Eksternal': ['sec-naskah'],
  'Memo':['sec-memo'], 'Nota Dinas':['sec-memo','sec-naskah'],
  'SPPD':['sec-sppd'],
  'Surat Tugas':['sec-tugas'],
  'Surat Tugas Pendelegasian':['sec-delegasi'],
  'Surat Keterangan':['sec-keterangan'],
  'Surat Pernyataan':['sec-pernyataan'],
  'Surat Kuasa':['sec-kuasa'],
  'Surat Pengantar':['sec-pengantar'],
  'Surat Keputusan':['sec-keputusan'],
  'SK':['sec-keputusan'],
  'Surat Undangan Dinas':['sec-undangan'],
  'Surat Statuta':['sec-statuta'],
  'MOU':['sec-pihak-kedua'],
  'PKS':['sec-pihak-kedua'],
  'Kontrak':['sec-pihak-kedua'],
  'SPK':['sec-spk'],
  'Invoice':['sec-invoice'],
};
const BODY_PLACEHOLDER = {
  'Surat': 'Tulis isi surat di sini...',
  'Surat Resmi': 'Tulis isi surat resmi di sini...',
  'Surat Formal': 'Tulis isi surat formal di sini...',
  'Surat Instruksi': 'Tulis instruksi-instruksi di sini...',
  'Surat Edaran': 'Tulis poin-poin edaran di sini...',
  'Surat Rahasia': 'Tulis isi surat rahasia di sini...',
  'Surat Konfidensial': 'Tulis isi surat konfidensial di sini...',
  'Memo': 'Tulis isi memo / pesan di sini...',
  'Nota Dinas': 'Tulis isi nota dinas / keterangan tambahan di sini...',
  'SPPD': 'Keterangan tambahan perjalanan dinas (opsional)...',
  'Surat Tugas': 'Instruksi atau keterangan tambahan (opsional)...',
  'Surat Tugas Pendelegasian': 'Keterangan tambahan pendelegasian (opsional)...',
  'Surat Keterangan': 'Keterangan tambahan (opsional)...',
  'Surat Pernyataan': 'Kalimat penutup pernyataan (opsional)...',
  'Surat Kuasa': 'Keterangan tambahan surat kuasa (opsional)...',
  'Surat Pengantar': 'Keterangan / pesan pengantar di sini...',
  'Surat Keputusan': 'Keterangan atau catatan tambahan keputusan (opsional)...',
  'Surat Undangan Dinas': 'Keterangan tambahan undangan (opsional)...',
  'Surat Statuta': 'Ketentuan atau pasal-pasal tambahan (opsional)...',
  'MOU': 'Tulis pasal-pasal MOU (Pasal 1, Pasal 2, dst.) di sini...',
  'PKS': 'Tulis pasal-pasal PKS (Pasal 1, Pasal 2, dst.) di sini...',
  'Kontrak': 'Tulis pasal-pasal kontrak (Pasal 1, Pasal 2, dst.) di sini...',
  'SPK': 'Keterangan tambahan SPK (opsional)...',
  'Invoice': 'Catatan atau keterangan tambahan invoice (opsional)...',
};
// Auto-suggest kodeDiv, kodeLay, berkas per tipe surat (sesuai Pedoman Kodifikasi NIT)
const TIPE_TO_KODE = {
  'Surat':                      { div:'OPS', lay:'EXT', berkas:'OPS-EXT' },
  'Surat Resmi':                { div:'OPS', lay:'EXT', berkas:'OPS-EXT' },
  'Surat Formal':               { div:'OPS', lay:'EXT', berkas:'OPS-EXT' },
  'Surat Instruksi':            { div:'OPS', lay:'INT', berkas:'OPS-INT' },
  'Surat Edaran':               { div:'OPS', lay:'EXT', berkas:'OPS-EXT' },
  'Surat Rahasia':              { div:'OPS', lay:'INT', berkas:'OPS-INT' },
  'Surat Konfidensial':         { div:'OPS', lay:'INT', berkas:'OPS-INT' },
  'Memo':                       { div:'OPS', lay:'INT', berkas:'OPS-INT' },
  'Nota Dinas':                 { div:'OPS', lay:'INT', berkas:'OPS-INT' },
  'SPPD':                       { div:'OPS', lay:'ST',  berkas:'OPS-ST'  },
  'Surat Tugas':                { div:'OPS', lay:'ST',  berkas:'OPS-ST'  },
  'Surat Tugas Pendelegasian':  { div:'OPS', lay:'ST',  berkas:'OPS-ST'  },
  'Surat Keterangan':           { div:'OPS', lay:'SRT', berkas:'OPS-SRT' },
  'Surat Pernyataan':           { div:'OPS', lay:'INT', berkas:'OPS-INT' },
  'Surat Kuasa':                { div:'OPS', lay:'INT', berkas:'OPS-INT' },
  'Surat Pengantar':            { div:'OPS', lay:'EXT', berkas:'OPS-EXT' },
  'Surat Keputusan':            { div:'DIR', lay:'SK',  berkas:'DIR-SK'  },
  'Surat Undangan Dinas':       { div:'OPS', lay:'EXT', berkas:'OPS-EXT' },
  'Surat Statuta':              { div:'DIR', lay:'SK',  berkas:'DIR-SK'  },
  'MOU':                        { div:'BIS', lay:'PKS', berkas:'BIS-PKS' },
  'PKS':                        { div:'BIS', lay:'PKS', berkas:'BIS-PKS' },
  'Kontrak':                    { div:'BIS', lay:'SPK', berkas:'BIS-SPK' },
  'SPK':                        { div:'OPS', lay:'SPK', berkas:'BIS-SPK' },
  'Invoice':                    { div:'BIS', lay:'INV', berkas:'BIS-INV' },
};
// Kode direksi untuk SK; kode divisi untuk surat internal
const KODE_DIREKSI = ['CEO','CTO','CMO','COO'];
const KODE_DIVISI  = ['KOM','PLAN','TECH','MP'];

function filterKodeDirOptions(isSK) {
  const sel = document.getElementById('kodeDirSel');
  const label = document.getElementById('kodeDirLabel');
  if (!sel) return;
  let firstVisible = null;
  Array.from(sel.options).forEach(opt => {
    const group = opt.dataset.group;
    const show = isSK ? (group === 'direksi') : (group !== 'direksi');
    opt.style.display = show ? '' : 'none';
    if (show && !firstVisible) firstVisible = opt;
  });
  // Pilih opsi pertama yang terlihat jika opsi aktif tersembunyi
  if (sel.selectedOptions.length && sel.selectedOptions[0].style.display === 'none') {
    if (firstVisible) sel.value = firstVisible.value;
  }
  if (label) label.innerHTML = isSK
    ? 'Kode Direksi <span class="req">*</span>'
    : 'Kode Direksi <span class="req">*</span>';
}

function updateNomorPreview(val) {
  const preview = document.getElementById('nomorPreview');
  if (!preview) return;
  if (val === 'SK' || val === 'Surat Keputusan') {
    preview.textContent = 'NNN / SK / CEO|CTO|CMO|COO / NIT / BLN / THN';
  } else if (['MoU','MoA','Contract','IA','PKS','SPK'].includes(val)) {
    const codeMap = {'MoU':'MOU','MoA':'MOA','Contract':'CONTRACT','IA':'IA','PKS':'PKS','SPK':'SPK'};
    preview.textContent = `NNN / ${codeMap[val]||val} / NIT / BLN / THN`;
  } else if (val === 'Surat Eksternal') {
    preview.textContent = '— (belum ditentukan) —';
  } else {
    preview.textContent = 'NNN / DIV / NIT / BLN / THN';
  }
}

function onTipeChange(val) {
  currentTipe = val;
  document.getElementById('tipeSuratHidden').value = val;
  document.getElementById('infoTipe').textContent = val;
  ALL_SECS.forEach(id => document.getElementById(id).style.display = 'none');
  (TIPE_SECS[val] || []).forEach(id => document.getElementById(id).style.display = '');
  // Show nota budget only for Nota Dinas
  const notaBudget = document.getElementById('sec-nota-budget');
  if (notaBudget) notaBudget.style.display = val === 'Nota Dinas' ? '' : 'none';
  // Naskah: field eksternal & catatan BSrE hanya untuk Surat Eksternal
  const naskahExt  = document.getElementById('naskah-ext-fields');
  const bsreWrap   = document.getElementById('naskah-bsre-wrap');
  if (naskahExt) naskahExt.style.display = val === 'Surat Eksternal' ? '' : 'none';
  if (bsreWrap)  bsreWrap.style.display  = val === 'Surat Eksternal' ? '' : 'none';
  const nilaiKontrak = document.getElementById('sec-nilai-kontrak');
  if (nilaiKontrak) nilaiKontrak.style.display = val === 'Kontrak' ? '' : 'none';
  // Filter kodeDir options: direksi untuk SK, divisi untuk lainnya
  const isSK = (val === 'SK' || val === 'Surat Keputusan');
  filterKodeDirOptions(isSK);
  // Tampilkan form Pihak Kedua untuk dokumen khusus
  const TIPE_KHUSUS_JS = ['MoU','MoA','Contract','IA','PKS','SPK','SK','Surat Keputusan'];
  const p2form = document.getElementById('sec-pihak-kedua-form');
  if (p2form) p2form.style.display = TIPE_KHUSUS_JS.includes(val) ? '' : 'none';
  // Update preview nomor surat di sidebar
  updateNomorPreview(val);
  // Update quill placeholder
  const ph = BODY_PLACEHOLDER[val] || 'Tulis isi surat di sini...';
  quill.root.dataset.placeholder = ph;
}

// ── JENIS ──
let currentJenis = 'internal';
function setJenis(j) {
  currentJenis = j;
  document.getElementById('jenisHidden').value = j;
  const ti = document.getElementById('tabInternal');
  const te = document.getElementById('tabEksternal');
  if (ti) ti.classList.toggle('active', j === 'internal');
  if (te) te.classList.toggle('active', j === 'eksternal');
  document.getElementById('internalSection').style.display = j === 'internal' ? '' : 'none';
  document.getElementById('eksternalSection').style.display = j === 'eksternal' ? '' : 'none';
}

// ── EXTERNAL RECIPIENTS ──
let extList = [];
function addExt() {
  const sel = document.getElementById('perusahaanPicker');
  const id = sel ? sel.value : '';
  if (!id) { alert('Pilih perusahaan dari daftar terlebih dahulu.'); return; }
  const p = _perusahaanData.find(x => x._id === id);
  if (!p) return;
  if (extList.find(r => r.perusahaanId === id)) { alert('Perusahaan sudah ditambahkan.'); return; }
  extList.push({ perusahaanId: id, name: p.nama, email: p.email || '' });
  renderExtList();
  sel.value = '';
}
function removeExt(id) { extList = extList.filter(r => r.perusahaanId !== id); renderExtList(); }
function renderExtList() {
  const list = document.getElementById('extList');
  const empty = document.getElementById('extEmpty');
  list.innerHTML = ''; empty.style.display = extList.length ? 'none' : '';
  extList.forEach(r => {
    const d = document.createElement('div'); d.className = 'ext-item';
    const emailInfo = r.email ? ` <span style="color:var(--gray-400);font-size:11.5px;">&lt;${esc(r.email)}&gt;</span>` : '';
    d.innerHTML = `<span class="ext-item-name">${esc(r.name)}${emailInfo}</span><button type="button" class="ext-item-del" onclick="removeExt('${esc(r.perusahaanId)}')">&times;</button>`;
    list.appendChild(d);
  });
  document.getElementById('externalRecipientsHidden').value = JSON.stringify(extList);
}
function esc(s) { return String(s).replace(/[&<>"']/g, c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])); }

// ── DYNAMIC ROWS / LISTS ──

// Budget rows (Nota Dinas)
let budgetRows = [];
function addBudgetRow() {
  const idx = budgetRows.length + 1;
  const id = 'br_' + Date.now();
  budgetRows.push(id);
  const tr = document.createElement('tr'); tr.id = 'row_' + id;
  tr.innerHTML = `<td style="text-align:center;font-size:12px;color:var(--gray-500);">${idx}</td>
    <td><input type="text" id="br_uraian_${id}" class="form-control" placeholder="Uraian kebutuhan..." style="min-width:200px;"></td>
    <td><input type="text" id="br_jumlah_${id}" class="form-control" placeholder="Rp ..."></td>
    <td><button type="button" class="dyn-row-del" onclick="delRow('row_${id}','budget','${id}')">×</button></td>`;
  document.getElementById('budgetBody').appendChild(tr);
}
function delRow(rowId, type, id) {
  document.getElementById(rowId)?.remove();
  if (type === 'budget') budgetRows = budgetRows.filter(r => r !== id);
  else if (type === 'pengantar') pengantarRows = pengantarRows.filter(r => r !== id);
  else if (type === 'staAnggota') staAnggotaRows = staAnggotaRows.filter(r => r !== id);
  else if (type === 'invoice') { invRows = invRows.filter(r => r !== id); recalcInv(); }
  else if (type === 'tugas') { tugasRows = tugasRows.filter(r => r !== id); }
  renumberRows(type);
}
function renumberRows(type) {
  const bodyMap = { budget:'budgetBody', pengantar:'pengantarBody', staAnggota:'staAnggotaBody', invoice:'invBody', tugas:'tugasBody' };
  const body = document.getElementById(bodyMap[type]);
  if (!body) return;
  Array.from(body.querySelectorAll('tr')).forEach((r, i) => { const numCell = r.querySelector('td:first-child'); if(numCell) numCell.textContent = i+1; });
}

// Pengantar rows
let pengantarRows = [];
function addPengantarRow() {
  const id = 'pr_' + Date.now(); pengantarRows.push(id);
  const idx = pengantarRows.length;
  const tr = document.createElement('tr'); tr.id = 'row_' + id;
  tr.innerHTML = `<td style="text-align:center;font-size:12px;color:var(--gray-500);">${idx}</td>
    <td><input type="text" id="pr_nama_${id}" class="form-control" placeholder="Nama dokumen..." style="min-width:180px;"></td>
    <td><input type="text" id="pr_jumlah_${id}" class="form-control" placeholder="cth: 1 berkas"></td>
    <td><input type="text" id="pr_ket_${id}" class="form-control" placeholder="Asli / Fotokopi"></td>
    <td><button type="button" class="dyn-row-del" onclick="delRow('row_${id}','pengantar','${id}')">×</button></td>`;
  document.getElementById('pengantarBody').appendChild(tr);
}

// Statuta anggota rows
let staAnggotaRows = [];
function addStaAnggota() {
  const id = 'sa_' + Date.now(); staAnggotaRows.push(id);
  const idx = staAnggotaRows.length;
  const tr = document.createElement('tr'); tr.id = 'row_' + id;
  tr.innerHTML = `<td style="text-align:center;font-size:12px;color:var(--gray-500);">${idx}</td>
    <td><input type="text" id="sa_nama_${id}" class="form-control" placeholder="Nama anggota" style="min-width:160px;"></td>
    <td><input type="text" id="sa_jabatan_${id}" class="form-control" placeholder="Jabatan dalam tim"></td>
    <td><button type="button" class="dyn-row-del" onclick="delRow('row_${id}','staAnggota','${id}')">×</button></td>`;
  document.getElementById('staAnggotaBody').appendChild(tr);
}

// Dynamic list helpers
function addListItem(listId, placeholder) {
  const list = document.getElementById(listId);
  const idx = list.children.length + 1;
  const div = document.createElement('div'); div.className = 'dyn-list-item';
  div.innerHTML = `<span class="item-num">${idx}.</span>
    <input type="text" class="form-control" placeholder="${placeholder}">
    <button type="button" class="dyn-row-del" onclick="this.parentElement.remove();renumberList('${listId}')">×</button>`;
  list.appendChild(div);
}
function renumberList(listId) {
  Array.from(document.getElementById(listId).children).forEach((item, i) => {
    const num = item.querySelector('.item-num'); if(num) num.textContent = (i+1)+'.';
  });
}
// Delegation lingkup
function addDelLingkup() { addListItem('del_lingkup_list','Lingkup pendelegasian...'); }
function addDelKetentuan() { addListItem('del_ketentuan_list','Ketentuan pendelegasian...'); }
// Pernyataan poin
function addPryPoin() { addListItem('pry_poin_list','Pernyataan nomor '+(document.getElementById('pry_poin_list').children.length+1)+'...'); }
// Kuasa lingkup
function addKuasaLingkup() { addListItem('kuasa_lingkup_list','Ruang lingkup kuasa...'); }
// SK items
function addSkItem(type) { addListItem('sk_'+type+'_list','Poin '+type+'...'); }
// Statuta misi
function addStaMisi() { addListItem('sta_misi_list','Pernyataan misi...'); }
// SPK rows
let spkRows = [];
function addSpkRow() {
  const id = 'spk_' + Date.now();
  spkRows.push(id);
  const idx = spkRows.length;
  const tr = document.createElement('tr');
  tr.id = 'spkrow_' + id;
  tr.innerHTML = `<td style="text-align:center;padding:6px 4px;">${idx}</td>
    <td><input type="text" id="spk_uraian_${id}" class="form-control" placeholder="Uraian pekerjaan..."></td>
    <td><input type="text" id="spk_ket_${id}" class="form-control" placeholder="cth: Menyeluruh"></td>
    <td><button type="button" class="btn-del-row" onclick="document.getElementById('spkrow_${id}').remove();spkRows=spkRows.filter(x=>x!=='${id}')">×</button></td>`;
  document.getElementById('spkBody').appendChild(tr);
}

// Tugas rows (pelaksana)
let tugasRows = [];
function addTugasRow() {
  const id = 'tgs_' + Date.now();
  tugasRows.push(id);
  const idx = tugasRows.length;
  const tr = document.createElement('tr');
  tr.id = 'tugasrow_' + id;
  tr.innerHTML = `
    <td style="text-align:center;padding:6px 4px;">${idx}</td>
    <td><input type="text" id="tgs_nama_${id}" class="form-control" placeholder="Nama lengkap..."></td>
    <td><input type="text" id="tgs_jabatan_${id}" class="form-control" placeholder="Jabatan..."></td>
    <td><button type="button" class="btn-del-row" onclick="delRow('tugasrow_${id}','tugas','${id}')">×</button></td>`;
  document.getElementById('tugasBody').appendChild(tr);
  document.getElementById('tgs_nama_' + id).focus();
}

// Invoice rows
let invRows = [];
function addInvRow() {
  const id = 'inv_' + Date.now();
  invRows.push(id);
  const idx = invRows.length;
  const tr = document.createElement('tr');
  tr.id = 'invrow_' + id;
  tr.innerHTML = `
    <td style="text-align:center;padding:6px 4px;">${idx}</td>
    <td><input type="text" id="inv_desc_${id}" class="form-control" placeholder="Nama layanan / item..."></td>
    <td><input type="number" id="inv_qty_${id}" class="form-control" placeholder="1" min="0" step="any" oninput="recalcInv()"></td>
    <td><input type="text" id="inv_sat_${id}" class="form-control" placeholder="unit"></td>
    <td><input type="text" id="inv_harga_${id}" class="form-control" placeholder="0" oninput="recalcInv()"></td>
    <td id="inv_jml_${id}" style="padding:6px 8px;font-weight:500;font-size:13px;white-space:nowrap;">Rp 0</td>
    <td><button type="button" class="btn-del-row" onclick="delRow('invrow_${id}','invoice','${id}')">×</button></td>`;
  document.getElementById('invBody').appendChild(tr);
}
function parseRp(s) { return parseFloat(String(s).replace(/[^0-9.]/g,'')) || 0; }
function fmtRp(n) { return 'Rp ' + Math.round(n).toLocaleString('id-ID'); }
function recalcInv() {
  let subtotal = 0;
  invRows.forEach(id => {
    const qty   = parseFloat(document.getElementById('inv_qty_'+id)?.value) || 0;
    const harga = parseRp(document.getElementById('inv_harga_'+id)?.value);
    const jml   = qty * harga;
    const jmlEl = document.getElementById('inv_jml_'+id);
    if (jmlEl) jmlEl.textContent = fmtRp(jml);
    subtotal += jml;
  });
  const pajak   = parseFloat(document.getElementById('inv_pajak')?.value) || 0;
  const diskon  = parseRp(document.getElementById('inv_diskon')?.value);
  const pajakAmt = subtotal * pajak / 100;
  const total   = subtotal + pajakAmt - diskon;
  document.getElementById('inv_subtotal_display').textContent = fmtRp(subtotal);
  document.getElementById('inv_pajak_display').textContent    = fmtRp(pajakAmt);
  document.getElementById('inv_diskon_display').textContent   = fmtRp(diskon);
  document.getElementById('inv_total_display').textContent    = fmtRp(total < 0 ? 0 : total);
}

// ── BUTIR BUILDER (Nota Dinas & Surat Eksternal) ──
function addButir() {
  const list = document.getElementById('butirList');
  const card = document.createElement('div');
  card.className = 'butir-card';
  card.style = 'border:1px solid var(--gray-200);border-radius:8px;padding:12px;margin-bottom:10px;background:var(--gray-50);';
  const no = list.children.length + 1;
  card.innerHTML = `
    <div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:8px;">
      <span class="butir-no" style="font-weight:600;font-size:12.5px;color:var(--gray-600);">Butir ${no}</span>
      <button type="button" class="dyn-row-del" onclick="this.closest('.butir-card').remove();renumberButir()">×</button>
    </div>
    <textarea class="form-control textarea butir-teks" rows="2" placeholder="Teks butir, cth: Mendasari Peraturan ... / Sehubungan dengan ... disampaikan undangan ... pada:"></textarea>
    <div class="butir-sub" style="margin-top:8px;"></div>
    <button type="button" class="btn-add-row" style="margin-top:6px;" onclick="addButirSub(this)">+ Sub-poin (a, b, c)</button>
    <table class="dyn-table butir-tabel" style="margin-top:10px;display:none;">
      <thead><tr><th style="width:38%;">Label (cth: Hari/Tanggal)</th><th>Isi</th><th style="width:36px;"></th></tr></thead>
      <tbody></tbody>
    </table>
    <button type="button" class="btn-add-row" style="margin-top:6px;" onclick="addButirTabelRow(this)">+ Baris tabel acara/agenda</button>
    <textarea class="form-control textarea butir-akhir" rows="1" style="margin-top:8px;" placeholder="Teks penutup butir (opsional, cth: Selanjutnya, mohon ...)"></textarea>`;
  list.appendChild(card);
}
function renumberButir() {
  Array.from(document.querySelectorAll('#butirList .butir-card .butir-no')).forEach((el,i)=>{ el.textContent = 'Butir '+(i+1); });
}
function addButirSub(btn) {
  const wrap = btn.closest('.butir-card').querySelector('.butir-sub');
  const row = document.createElement('div');
  row.className = 'dyn-list-item';
  row.style = 'display:flex;align-items:center;gap:8px;margin-bottom:6px;';
  const letter = String.fromCharCode(97 + wrap.children.length);
  row.innerHTML = `<span class="item-num" style="width:18px;font-weight:600;">${letter}.</span>
    <input type="text" class="form-control" placeholder="Isi sub-poin...">
    <button type="button" class="dyn-row-del" onclick="const w=this.closest('.butir-sub');this.parentElement.remove();renumberButirSub(w)">×</button>`;
  wrap.appendChild(row);
}
function renumberButirSub(wrap) {
  Array.from(wrap.children).forEach((r,i)=>{ const n=r.querySelector('.item-num'); if(n) n.textContent=String.fromCharCode(97+i)+'.'; });
}
function addButirTabelRow(btn) {
  const table = btn.closest('.butir-card').querySelector('.butir-tabel');
  table.style.display = '';
  const tr = document.createElement('tr');
  tr.innerHTML = `<td><input type="text" class="form-control bt-label" placeholder="Hari/Tanggal"></td>
    <td><textarea class="form-control bt-value" rows="1" placeholder="Isi (boleh beberapa baris)"></textarea></td>
    <td><button type="button" class="dyn-row-del" onclick="this.closest('tr').remove()">×</button></td>`;
  table.querySelector('tbody').appendChild(tr);
}
function collectButir() {
  return Array.from(document.querySelectorAll('#butirList .butir-card')).map(card => {
    const teks = card.querySelector('.butir-teks').value.trim();
    const sub  = Array.from(card.querySelectorAll('.butir-sub input')).map(i=>i.value.trim()).filter(Boolean);
    const tabel = Array.from(card.querySelectorAll('.butir-tabel tbody tr')).map(tr=>({
      label: tr.querySelector('.bt-label').value.trim(),
      value: tr.querySelector('.bt-value').value.trim()
    })).filter(r => r.label || r.value);
    const teksAkhir = card.querySelector('.butir-akhir').value.trim();
    return { teks, sub, tabel, teksAkhir };
  }).filter(b => b.teks || b.sub.length || b.tabel.length);
}

// ── COLLECT SURAT DATA ──
function collectSuratData() {
  const sd = {};
  const t = currentTipe;

  if (t === 'Memo' || t === 'Nota Dinas') {
    sd.kepada = v('memo_kepada');
    sd.klasifikasi = v('memo_klasifikasi');
    if (t === 'Nota Dinas') {
      sd.budgetRows = budgetRows.map(id => ({
        uraian: v('br_uraian_'+id),
        jumlah: v('br_jumlah_'+id)
      })).filter(r => r.uraian);
      sd.butir = collectButir();
      sd.ttdKota = v('naskah_ttd_kota');
      sd.ttdTanggal = v('naskah_ttd_tanggal');
      sd.ttdJabatan = v('naskah_ttd_jabatan');
    }
  }
  else if (t === 'Surat Eksternal') {
    sd.tglSurat = v('naskah_tgl_surat');
    sd.lampiran = v('naskah_lampiran');
    sd.kepadaAlamat = v('naskah_kepada_alamat');
    sd.butir = collectButir();
    sd.ttdKota = v('naskah_ttd_kota');
    sd.ttdTanggal = v('naskah_ttd_tanggal');
    sd.ttdJabatan = v('naskah_ttd_jabatan');
    sd.esignNote = document.getElementById('naskah_bsre') ? document.getElementById('naskah_bsre').checked : true;
  }
  else if (t === 'SPPD') {
    sd.namaPegawai = v('sppd_nama'); sd.jabatanPegawai = v('sppd_jabatan');
    sd.golongan = v('sppd_golongan'); sd.maksudPerjalanan = v('sppd_maksud');
    sd.alatAngkutan = v('sppd_angkutan'); sd.tempatBerangkat = v('sppd_asal');
    sd.tempatTujuan = v('sppd_tujuan'); sd.tanggalBerangkat = v('sppd_tgl_berangkat');
    sd.tanggalKembali = v('sppd_tgl_kembali'); sd.mataPenggunaan = v('sppd_mata_anggaran');
  }
  else if (t === 'Surat Tugas') {
    const _pJabatan = v('tugas_pemberi_preset') === 'lainnya' ? v('tugas_pemberi_jabatan_custom') : v('tugas_pemberi_preset');
    sd.jabatanPemberiTugas = _pJabatan;
    sd.namaPemberiTugas = v('tugas_pemberi_nama');
    sd.pelaksana = tugasRows.map(id => ({
      nama    : v('tgs_nama_' + id),
      jabatan : v('tgs_jabatan_' + id)
    })).filter(r => r.nama);
    sd.jenisTugas  = v('tugas_jenis');
    sd.tujuanTugas = v('tugas_tujuan');
    sd.lokasiTugas = v('tugas_lokasi');
    sd.tanggalTugas = v('tugas_tanggal');
    sd.waktuTugas  = v('tugas_waktu');
  }
  else if (t === 'Surat Tugas Pendelegasian') {
    sd.namaPenerima = v('del_nama'); sd.jabatanPenerima = v('del_jabatan');
    sd.lingkupDelegasi = getListValues('del_lingkup_list');
    sd.ketentuanDelegasi = getListValues('del_ketentuan_list');
  }
  else if (t === 'Surat Keterangan') {
    sd.namaDiterangkan = v('ket_nama'); sd.jabatanDiterangkan = v('ket_jabatan');
    sd.timDiterangkan = v('ket_tim'); sd.keperluan = v('ket_keperluan');
    sd.lokasiTugas = v('ket_lokasi'); sd.tanggalTugas = v('ket_tanggal');
  }
  else if (t === 'Surat Pernyataan') {
    sd.namaPenyataan = v('pry_nama'); sd.jabatanPernyataan = v('pry_jabatan');
    sd.timPernyataan = v('pry_tim');
    sd.poinPernyataan = getListValues('pry_poin_list');
  }
  else if (t === 'Surat Kuasa') {
    sd.namaPenerima = v('kuasa_nama'); sd.jabatanPenerima = v('kuasa_jabatan');
    sd.ruangLingkup = getListValues('kuasa_lingkup_list');
  }
  else if (t === 'Surat Pengantar') {
    sd.kepada = v('peng_kepada');
    sd.dokumenList = pengantarRows.map(id => ({
      nama: v('pr_nama_'+id), jumlah: v('pr_jumlah_'+id), keterangan: v('pr_ket_'+id)
    })).filter(r => r.nama);
  }
  else if (t === 'Surat Keputusan') {
    sd.menimbang = getListValues('sk_menimbang_list');
    sd.mengingat  = getListValues('sk_mengingat_list');
    sd.memutuskan = getListValues('sk_memutuskan_list');
  }
  else if (t === 'Surat Undangan Dinas') {
    sd.tanggalUndangan = v('und_tanggal'); sd.waktuUndangan = v('und_waktu');
    sd.tempatUndangan = v('und_tempat'); sd.agendaUndangan = v('und_agenda');
    sd.pakaianUndangan = v('und_pakaian');
  }
  else if (t === 'Surat Statuta') {
    sd.namaOrganisasi = v('sta_nama_org'); sd.visi = v('sta_visi');
    sd.misi = getListValues('sta_misi_list');
    sd.anggota = staAnggotaRows.map(id => ({
      nama: v('sa_nama_'+id), jabatan: v('sa_jabatan_'+id)
    })).filter(r => r.nama);
  }
  else if (t === 'MOU' || t === 'PKS' || t === 'Kontrak') {
    sd.namaPihakKedua = v('p2_nama');
    sd.jabatanPihakKedua = v('p2_jabatan');
    sd.instansiPihakKedua = v('p2_instansi');
    sd.alamatPihakKedua = v('p2_alamat');
    if (t === 'Kontrak') sd.nilaiKontrak = v('p2_nilai');
  }
  else if (t === 'SPK') {
    sd.namaTim = v('spk_tim');
    sd.ketuaTim = v('spk_ketua');
    sd.anggotaTim = v('spk_anggota');
    sd.pekerjaanSPK = spkRows.map(id => ({
      uraian: v('spk_uraian_'+id),
      keterangan: v('spk_ket_'+id)
    })).filter(r => r.uraian);
    sd.lokasiSPK = v('spk_lokasi');
    sd.tanggalSPK = v('spk_tanggal');
    sd.waktuSPK = v('spk_waktu');
  }
  else if (['Surat','Surat Resmi','Surat Formal','Surat Instruksi','Surat Rahasia','Surat Konfidensial'].includes(t)) {
    sd.jabatanPenerima = v('std_jabatan');
    sd.instansiPenerima = v('std_instansi');
    sd.alamatPenerima = v('std_alamat');
  }
  else if (['MoU','MoA','Contract','IA','PKS','SPK'].includes(t)) {
    const p2 = document.querySelector('[name="pihak2"]');
    const nilaiEl = document.querySelector('[name="nilaiKontrak"]');
    const tglMulai = document.querySelector('[name="tglMulai"]');
    const tglBerakhir = document.querySelector('[name="tglBerakhir"]');
    const deskEl = document.querySelector('[name="deskripsiDok"]');
    sd.pihak2 = p2?.value.trim() || '';
    sd.nilaiKontrak = nilaiEl?.value.replace(/\./g,'') || '';
    sd.tglMulai = tglMulai?.value || '';
    sd.tglBerakhir = tglBerakhir?.value || '';
    sd.deskripsi = deskEl?.value.trim() || '';
  }
  else if (t === 'Invoice') {
    sd.invKepada    = v('inv_kepada');
    sd.invAlamat    = v('inv_alamat');
    sd.invJatuhTempo = v('inv_jatuh_tempo');
    sd.invMetode    = v('inv_metode');
    sd.invCatatan   = v('inv_catatan');
    sd.invPajak     = parseFloat(document.getElementById('inv_pajak')?.value) || 0;
    sd.invDiskon    = parseRp(document.getElementById('inv_diskon')?.value);
    sd.invItems     = invRows.map(id => ({
      deskripsi : v('inv_desc_'+id),
      qty       : parseFloat(document.getElementById('inv_qty_'+id)?.value) || 0,
      satuan    : v('inv_sat_'+id),
      harga     : parseRp(document.getElementById('inv_harga_'+id)?.value)
    })).filter(r => r.deskripsi);
  }
  return sd;
}
function v(id) { const el = document.getElementById(id); return el ? el.value.trim() : ''; }
function onPemberiPresetChange() {
  const sel = document.getElementById('tugas_pemberi_preset');
  const custom = document.getElementById('tugas_pemberi_jabatan_custom');
  if (!sel || !custom) return;
  custom.style.display = sel.value === 'lainnya' ? '' : 'none';
  if (sel.value === 'lainnya') custom.focus();
}
function getListValues(listId) {
  return Array.from(document.getElementById(listId)?.querySelectorAll('input') || [])
    .map(i => i.value.trim()).filter(Boolean);
}

// ── SUBMIT ──
// Field wajib sebelum kirim/draf — beda untuk internal vs eksternal
function validateRequired() {
  const errors = [];
  const visible = el => el && el.offsetParent !== null;
  const blank   = el => !el.value || !el.value.trim();

  // Isi surat: editor utama ATAU minimal satu butir terisi
  const bodyText    = (typeof quill !== 'undefined') ? quill.getText().trim() : '';
  const butirFilled = Array.from(document.querySelectorAll('#butirList .butir-teks')).some(t => t.value.trim());
  const hasIsi      = bodyText.length > 0 || butirFilled;

  if (currentJenis === 'eksternal') {
    const tgl   = document.getElementById('naskah_tgl_surat');
    const tuju  = document.getElementById('naskah_kepada_alamat');
    if (visible(tgl)  && blank(tgl))  errors.push('Tanggal Surat');
    if (visible(tuju) && blank(tuju)) errors.push('Kepada Yth.');
    if (extList.length === 0)         errors.push('Penerima (pihak eksternal)');
    if (!hasIsi)                      errors.push('Isi surat');
  } else {
    const kepada = document.getElementById('memo_kepada');
    const klas   = document.getElementById('memo_klasifikasi');
    if (visible(kepada) && blank(kepada)) errors.push('Kepada');
    if (visible(klas)   && blank(klas))   errors.push('Klasifikasi');
    const tertuju = document.querySelectorAll('#tertujuTags .selected-tag').length;
    if (!tertuju)                         errors.push('Tertuju (penerima internal)');
    if (!hasIsi)                          errors.push('Isi surat');
  }
  return errors;
}

function submitForm(action) {
  const errors = validateRequired();
  if (errors.length) {
    alert('Lengkapi dulu kolom wajib berikut sebelum ' + (action === 'send' ? 'dikirim' : 'disimpan sebagai draf') + ':\n\n• ' + errors.join('\n• '));
    return;
  }
  document.getElementById('formAction').value = action;
  document.getElementById('bodyHidden').value = quill.root.innerHTML;
  document.getElementById('externalRecipientsHidden').value = JSON.stringify(extList);
  document.getElementById('tipeSuratHidden').value = currentTipe;
  document.getElementById('suratDataHidden').value = JSON.stringify(collectSuratData());
  document.getElementById('composeForm').submit();
}

// ── DROPDOWN HELPERS ──
function toggleDropdown(id) { document.getElementById(id+'Dropdown').classList.toggle('open'); }
function searchDropdown(id, q) {
  document.getElementById(id+'Dropdown').querySelectorAll('.dropdown-opt').forEach(o => {
    o.style.display = !q||(o.dataset.search||'').includes(q.toLowerCase()) ? '' : 'none';
  });
}
function toggleTag(id, cb, name) {
  const tags = document.getElementById(id+'Tags');
  const ph   = document.getElementById(id+'Placeholder');
  if (cb.checked) {
    const t = document.createElement('span'); t.className = 'selected-tag'; t.dataset.value = cb.value;
    t.textContent = name.length>20 ? name.substring(0,18)+'…' : name;
    const b = document.createElement('button'); b.type='button'; b.innerHTML='×';
    b.onclick = e => { e.stopPropagation(); t.remove(); cb.checked=false; updatePh(id); };
    t.appendChild(b); tags.appendChild(t);
  } else { tags.querySelector('[data-value="'+cb.value+'"]')?.remove(); }
  updatePh(id);
}
function updatePh(id) {
  const ph = document.getElementById(id+'Placeholder');
  if(ph) ph.style.display = document.getElementById(id+'Tags').children.length ? 'none' : '';
}
document.addEventListener('click', e => {
  ['tertuju','tembusan'].forEach(id => {
    const w = document.getElementById(id+'Wrap');
    if (w && !w.contains(e.target)) document.getElementById(id+'Dropdown')?.classList.remove('open');
  });
});

// ── INIT JENIS DEFAULT ──
<% if (jenisDefault === 'eksternal') { %>
setJenis('eksternal');
<% } %>

// ── INIT TIPE DEFAULT (aktifkan section sesuai tipe terpilih) ──
(function initTipe(){
  const sel = document.getElementById('tipeSuratSelect');
  const val = sel && sel.value ? sel.value : (document.getElementById('tipeSuratHidden').value || 'Surat');
  onTipeChange(val);
  // Sediakan satu butir kosong awal untuk Nota Dinas / Surat Eksternal
  if ((val === 'Nota Dinas' || val === 'Surat Eksternal') && document.getElementById('butirList').children.length === 0) {
    addButir();
  }
})();

// ── PERUSAHAAN PICKER ──
let _perusahaanData = [];
async function loadPerusahaan() {
  try {
    const r = await fetch('/perusahaan-list');
    _perusahaanData = await r.json();
    const sel = document.getElementById('perusahaanPicker');
    if (!sel) return;
    _perusahaanData.forEach(p => {
      const o = document.createElement('option');
      o.value = p._id;
      o.textContent = (p.singkatan ? p.singkatan + ' — ' : '') + p.nama;
      sel.appendChild(o);
    });
  } catch(e) {}
}
loadPerusahaan();

</script>

<%- include('./partials/footer') %>
